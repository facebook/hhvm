{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}{{!

C++ header to store py3 specific Enum and Union type enum data

}}
{{> common/auto_generated_c}}
{{#if (not program.auto_migrate?)}}

#pragma once

#include <functional>
#include <folly/Range.h>

#include <thrift/lib/cpp2/gen/module_metadata_h.h>

#include "{{program.include_prefix}}{{program.cpp_gen_path}}/{{program.name}}_data.h"
#include "{{program.include_prefix}}{{program.cpp_gen_path}}/{{program.name}}_types.h"
namespace thrift {
namespace py3 {


{{#each program.py3_visible_structured_definitions as |struct|}}
  {{#if (not struct.union?)}}

template<>
inline void reset_field<{{#each program.cppNamespaces as |ns|}}::{{ns}}{{/each}}::{{struct.name}}>(
    {{#each program.cppNamespaces as |ns|}}::{{ns}}{{/each}}::{{struct.name}}& obj, uint16_t index) {
  switch (index) {
    {{#each (array.enumerate struct.py3_fields) as |field_idx field|}}
    case {{field_idx}}:
      {{#if field.reference?}}
      obj.{{field.cpp_name}}_ref(){{#if field.iobuf_ref?}}->{{#else}}.{{/if}}reset();
      {{#else}}
      obj.{{field.cpp_name}}_ref().copy_from(default_inst<{{#each program.cppNamespaces as |ns|}}::{{ns}}{{/each}}::{{struct.name}}>().{{field.cpp_name}}_ref());
      {{/if field.reference?}}
      return;
    {{/each}}
  }
}
  {{/if (not struct.union?)}}
{{/each}}
{{#each program.py3_visible_structured_definitions as |struct|}}

template<>
inline const std::unordered_map<std::string_view, std::string_view>& PyStructTraits<
    {{#each program.cppNamespaces as |ns|}}::{{ns}}{{/each}}::{{struct.name}}>::namesmap() {
  static const folly::Indestructible<NamesMap> map {
    {
      {{#each struct.py3_fields as |field|}}{{#if field.hasPyName?}}
      {"{{field.name}}", "{{field.py_name}}"},
      {{/if field.hasPyName?}}{{/each}}
    }
  };
  return *map;
}
{{/each}}
} // namespace py3
} // namespace thrift
{{/if (not program.auto_migrate?)}}
