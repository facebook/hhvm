# @generated by autocargo from //hphp/hack/src/package:package

[package]
name = "package"
version = "0.0.0"
edition = "2024"
repository = "https://github.com/facebook/hhvm"
license = "MIT"

[lib]
path = "../package.rs"

[dependencies]
anyhow = "1.0.98"
hash = { version = "0.0.0", path = "../../utils/hash" }
once_cell = "1.21"
serde = { version = "1.0.219", features = ["derive", "rc"] }
toml = { version = "0.9.11", features = ["preserve_order"] }

[dev-dependencies]
lazy_static = "1.5"
regex = "1.12.2"
