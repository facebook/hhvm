warning: Lint[5629] This `as` assertion will always fail and lead to an exception at runtime. The expression on the left has type `int` which shares no values with `bool`.
   --> as_invalid_type.php
 51 |     $_ = self::int() as bool; // error, never succeeds
    |          ^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 51 |     $_ = self::int() as bool; // error, never succeeds
    |          ^^^^^^^^^^^^^^^^^^^


warning: Lint[5628] This `as` assertion will always succeed and hence is redundant. The expression on the left has a type `bool` which is a subtype of `bool`.
   --> as_invalid_type.php
 52 |     $_ = self::bool() as bool; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 52 |     $_ = self::bool() as bool; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5628] This `as` assertion will always succeed and hence is redundant. The expression on the left has a type `float` which is a subtype of `num`.
   --> as_invalid_type.php
 53 |     $_ = self::float() as num; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 53 |     $_ = self::float() as num; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 54 |     $_ = self::num() as float;
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5629] This `as` assertion will always fail and lead to an exception at runtime. The expression on the left has type `float` which shares no values with `int`.
   --> as_invalid_type.php
 55 |     $_ = self::float() as int; // error, never succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 55 |     $_ = self::float() as int; // error, never succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 56 |     $_ = self::arraykey() as num;
    |          ^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5628] This `as` assertion will always succeed and hence is redundant. The expression on the left has a type `string` which is a subtype of `arraykey`.
   --> as_invalid_type.php
 57 |     $_ = self::string() as arraykey; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 57 |     $_ = self::string() as arraykey; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5629] This `as` assertion will always fail and lead to an exception at runtime. The expression on the left has type `string` which shares no values with `int`.
   --> as_invalid_type.php
 58 |     $_ = self::string() as int; // error, never succeeds
    |          ^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 58 |     $_ = self::string() as int; // error, never succeeds
    |          ^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 59 |     $_ = self::mixed() as int;
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5628] This `as` assertion will always succeed and hence is redundant. The expression on the left has a type `int` which is a subtype of `mixed`.
   --> as_invalid_type.php
 60 |     $_ = self::int() as mixed; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 60 |     $_ = self::int() as mixed; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 61 |     $_ = self::nullable(self::string()) as arraykey;
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5628] This `as` assertion will always succeed and hence is redundant. The expression on the left has a type `?string` which is a subtype of `?string`.
   --> as_invalid_type.php
 62 |     $_ = self::nullable(self::string()) as ?string; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 62 |     $_ = self::nullable(self::string()) as ?string; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 63 |     $_ = self::nullable(self::string()) as ?int;
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 64 |     $_ = self::stringEnum() as string;
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 65 |     $_ = self::stringEnum() as ?string;
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 66 |     $_ = self::stringEnum() as int;
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5628] This `as` assertion will always succeed and hence is redundant. The expression on the left has a type `null` which is a subtype of `mixed`.
   --> as_invalid_type.php
 67 |     $_ = self::null() as mixed; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 67 |     $_ = self::null() as mixed; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5628] This `as` assertion will always succeed and hence is redundant. The expression on the left has a type `null` which is a subtype of `null`.
   --> as_invalid_type.php
 68 |     $_ = self::null() as null; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 68 |     $_ = self::null() as null; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5628] This `as` assertion will always succeed and hence is redundant. The expression on the left has a type `null` which is a subtype of `?int`.
   --> as_invalid_type.php
 69 |     $_ = self::null() as ?int; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 69 |     $_ = self::null() as ?int; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 70 |     $_ = self::nullable(self::int()) as null;
    |          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^


warning: Lint[5629] This `as` assertion will always fail and lead to an exception at runtime. The expression on the left has type `null` which shares no values with `int`.
   --> as_invalid_type.php
 71 |     $_ = self::null() as int; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 71 |     $_ = self::null() as int; // error, always succeeds
    |          ^^^^^^^^^^^^^^^^^^^


warning: Lint[5629] This `as` assertion will always fail and lead to an exception at runtime. The expression on the left has type `int` which shares no values with `null`.
   --> as_invalid_type.php
 72 |     $_ = self::int() as null; // error, never succeeds
    |          ^^^^^^^^^^^^^^^^^^^


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through `as`. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change.
   --> as_invalid_type.php
 72 |     $_ = self::int() as null; // error, never succeeds
    |          ^^^^^^^^^^^^^^^^^^^


