{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}{{!

Generates a top-level file to be imported in the user's service code. It provides
wrappers for each of the service handlers that the user is then able to extend.

The generated file is pretty big, but the bulk of the generation is done inside
the services/promise and services/callback partials.

}}
{{#import "common/pep484" as pep484}}
{{#import "common/services" as services}}
{{#import "common/types" as types}}
{{#import "common/common" as common}}
{{> common/auto_generated_py}}
{{#if (not program.auto_migrate?)}}

cimport cython
from typing import AsyncIterator
from cpython.version cimport PY_VERSION_HEX
from libc.stdint cimport (
    int8_t as cint8_t,
    int16_t as cint16_t,
    int32_t as cint32_t,
    int64_t as cint64_t,
)
from libcpp.memory cimport shared_ptr, make_shared, unique_ptr
from libcpp.string cimport string
from libcpp cimport bool as cbool
from cpython cimport bool as pbool
from libcpp.vector cimport vector
from libcpp.set cimport set as cset
from libcpp.map cimport map as cmap
from libcpp.utility cimport move as cmove
from libcpp.pair cimport pair
from cython.operator cimport dereference as deref
from cpython.ref cimport PyObject
from thrift.python.exceptions cimport (
    ApplicationError as __ApplicationError,
    cTApplicationException,
    cTApplicationExceptionType__UNKNOWN,
)
from thrift.py3.server cimport ServiceInterface
from thrift.python.server_impl.request_context cimport (
    Cpp2RequestContext,
    RequestContext,
    THRIFT_REQUEST_CONTEXT as __THRIFT_REQUEST_CONTEXT,
)
from thrift.python.server_impl.request_context import RequestContext
from folly cimport (
  cFollyPromise,
  cFollyUnit,
  c_unit,
)
from thrift.python.common cimport (
    cThriftServiceMetadataResponse as __fbthrift_cThriftServiceMetadataResponse,
    ServiceMetadata,
    MetadataBox as __MetadataBox,
)

from thrift.py3.types cimport (
    make_unique,
    deref_const as __deref_const,
)

cimport folly.futures
from folly.executor cimport get_executor
cimport folly.iobuf as _fbthrift_iobuf
import folly.iobuf as _fbthrift_iobuf
from folly.iobuf cimport move as move_iobuf
from folly.memory cimport to_shared_ptr as __to_shared_ptr

{{#if program.has_stream?}}
from libcpp.optional cimport optional
from thrift.py3.stream cimport cServerStream, cServerStreamPublisher, cResponseAndServerStream, createResponseAndServerStream, createAsyncIteratorFromPyIterator, pythonFuncToCppFunc, ServerStream, ServerPublisher
{{/if program.has_stream?}}
cimport {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.types as _{{#each program.py3Namespaces as |ns|}}{{ns}}_{{/each}}{{program.name}}_types
cimport {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.cbindings as _{{#each program.py3Namespaces as |ns|}}{{ns}}_{{/each}}{{program.name}}_cbindings
{{#if program.inplace_migrate?}}
cimport {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.thrift_converter as _{{#each program.py3Namespaces as |ns|}}{{ns}}_{{/each}}{{program.name}}_thrift_converter
{{/if program.inplace_migrate?}}
import {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.types as _{{#each program.py3Namespaces as |ns|}}{{ns}}_{{/each}}{{program.name}}_types
{{#each program.includeNamespaces as |include|}}
  {{#if include.import_services?}}
cimport {{include.modulePathPeriodSeparated}}.services as _{{include.modulePathUnderscoreSeparated}}_services
import {{include.modulePathPeriodSeparated}}.services as _{{include.modulePathUnderscoreSeparated}}_services
  {{/if include.import_services?}}
  {{#if include.has_types?}}
import {{include.modulePathPeriodSeparated}}.types as _{{include.modulePathUnderscoreSeparated}}_types
cimport {{include.modulePathPeriodSeparated}}.types as _{{include.modulePathUnderscoreSeparated}}_types
cimport {{include.modulePathPeriodSeparated}}.cbindings as _{{include.modulePathUnderscoreSeparated}}_cbindings
    {{#if program.inplace_migrate?}}
cimport {{include.modulePathPeriodSeparated}}.thrift_converter as _{{include.modulePathUnderscoreSeparated}}_thrift_converter
    {{/if program.inplace_migrate?}}
  {{/if include.has_types?}}
{{/each}}

cimport {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.services_interface as _fbthrift_services_interface

import asyncio
import functools
import sys
import traceback
import types as _py_types

{{#each program.services as |service|}}
from {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.services_wrapper cimport c{{service.name}}Interface
{{/each}}
{{#each program.stream_types as |type|}}
cdef class ServerPublisher_{{#partial services.cython_cpp_type_ident type=type}}(ServerPublisher):
    cdef unique_ptr[cServerStreamPublisher[{{!
            }}{{#partial types.cython_cpp_type type=type}}{{!
        }}]] cPublisher

    def complete(ServerPublisher self):
        cmove(deref(self.cPublisher)).complete()

    # Calling this send instead of the wrapped method name of next because next is
    # a python keyword and makes the linter complain
    def send(ServerPublisher self, {{#partial types.cython_python_type type=type}} item):
        deref(self.cPublisher).next(<{{#partial types.cython_cpp_type type=type}}?>{{#partial types.cython_python_to_cpp_item type=type}})

    @staticmethod
    cdef _fbthrift_create(cServerStreamPublisher[{{!
            }}{{#partial types.cython_cpp_type type=type}}{{!
        }}] cPublisher):
        cdef ServerPublisher_{{#partial services.cython_cpp_type_ident type=type}} inst = ServerPublisher_{{#partial services.cython_cpp_type_ident type=type}}.__new__(ServerPublisher_{{#partial services.cython_cpp_type_ident type=type}})
        inst.cPublisher = make_unique[cServerStreamPublisher[{{!
            }}{{#partial types.cython_cpp_type type=type}}{{!
        }}]](cmove(cPublisher))
        return inst

cdef class ServerStream_{{#partial services.cython_cpp_type_ident type=type}}(ServerStream):
    cdef unique_ptr[cServerStream[{{!
            }}{{#partial types.cython_cpp_type type=type}}{{!
        }}]] cStream

    @staticmethod
    cdef _fbthrift_create(cServerStream[{{!
            }}{{#partial types.cython_cpp_type type=type}}{{!
        }}] cStream):
        cdef ServerStream_{{#partial services.cython_cpp_type_ident type=type}} inst = ServerStream_{{#partial services.cython_cpp_type_ident type=type}}.__new__(ServerStream_{{#partial services.cython_cpp_type_ident type=type}})
        inst.cStream = make_unique[cServerStream[{{!
            }}{{#partial types.cython_cpp_type type=type}}{{!
        }}]](cmove(cStream))
        return inst
{{/each}}

{{#each program.services as |service|}}
{{#each service.supportedFunctions as |function|}}
{{#if function.stream?}}
async def runGenerator_{{service.name}}_{{function.name}}(object generator, Promise_{{#partial services.cython_cpp_type_ident type=function.stream.elem_type}}_Stream promise):
    try:
        item = await generator.asend(None)
    except StopAsyncIteration:
        promise.cPromise.setValue(optional[{{#partial types.cython_cpp_type type=function.stream.elem_type}}]())
    {{#each function.stream.exceptions as |field|}}
    except {{#partial types.python_type type=field.type}} as ex:
        promise.cPromise.setException({{!
        }}{{#if (not program.inplace_migrate?)}}{{!
          }}deref((<{{#partial types.cython_python_type type=field.type}}> ex).{{#partial types.cpp_obj}}){{!
        }}{{#else if field.type.structured?}}{{!
          }}{{field.type.capi_converter_path}}.{{field.type.true_type.name}}_convert_to_cpp(ex._to_python()){{!
        }}{{/if (not program.inplace_migrate?)}})
    {{/each}}
    except __ApplicationError as ex:
        # If the handler raised an ApplicationError convert it to a C++ one
        promise.cPromise.setException(cTApplicationException(
            ex.type.value, ex.message.encode('UTF-8')
        ))
    except Exception as ex:
        print(
            "Unexpected error in {{service.name}}.{{function.name}}:",
            file=sys.stderr)
        traceback.print_exc()
        promise.cPromise.setException(cTApplicationException(
            cTApplicationExceptionType__UNKNOWN, repr(ex).encode('UTF-8')
        ))
    except asyncio.CancelledError as ex:
        print("Coroutine was cancelled in service handler {{service.name}}.{{function.name}}:", file=sys.stderr)
        traceback.print_exc()
        promise.cPromise.setException(cTApplicationException(
            cTApplicationExceptionType__UNKNOWN, (f'Application was cancelled on the server with message: {str(ex)}').encode('UTF-8')
        ))
    else:
        promise.cPromise.setValue(optional[{{#partial types.cython_cpp_type type=function.stream.elem_type}}](<{{#partial types.cython_cpp_type type=function.stream.elem_type}}?>{{#partial types.cython_python_to_cpp_item type=function.stream.elem_type}}))

cdef void getNextGenerator_{{service.name}}_{{function.name}}(object generator, cFollyPromise[optional[{{#partial types.cython_cpp_type type=function.stream.elem_type}}]] cPromise) noexcept:
    cdef Promise_{{#partial services.cython_cpp_type_ident type=function.stream.elem_type}}_Stream __promise = Promise_{{#partial services.cython_cpp_type_ident type=function.stream.elem_type}}_Stream._fbthrift_create(cmove(cPromise))
    asyncio.get_event_loop().create_task(
        runGenerator_{{service.name}}_{{function.name}}(
            generator,
            __promise
        )
    )
{{/if function.stream?}}
{{/each}}
{{/each}}
{{> services/promise}}
{{> stream/stream_promises}}

{{#each program.services as |service|}}
cdef object _{{service.name}}_annotations = _py_types.MappingProxyType({
{{#each service.unstructured_annotations as |annot|}}
    {{#partial common.annotation annot=annot}}
{{/each}}
})


@cython.auto_pickle(False)
cdef class {{service.name}}Interface(
    {{#let parent = service.extends}}
    {{#if (object.notnull? parent)}}
      {{#if parent.externalProgram?}}
    _{{#each parent.program.py3Namespaces as |ns|}}{{ns}}_{{/each}}{{!
        }}{{parent.program.name}}_services.{{/if parent.externalProgram?}}{{parent.name}}Interface
    {{#else}}
    ServiceInterface
    {{/if (object.notnull? parent)}}
):
    annotations = _{{service.name}}_annotations

    def __cinit__(self):
        self._cpp_obj = c{{service.name}}Interface(
            <PyObject *> self,
            get_executor()
        )
{{#each service.supportedFunctions as |function|}}

    {{! horrific workaround to expose python annotations on thrift py3 service functions}}
    _fbthrift_annotations_DO_NOT_USE_{{function.name}} = {
        'return': '{{#if function.sink_or_stream?}}None{{#else}}{{#partial pep484.type_period_separated type=function.return_type}}{{/if}}',
        {{#each function.params.fields as |param|}}'{{param.py_name}}': '{{#partial pep484.type_period_separated type=param.type}}', {{/each}}
    }

    async def {{function.name}}(
            self{{#each function.params.fields as |param|}},
            {{param.py_name}}{{/each}}):
        raise NotImplementedError("async def {{function.name}} is not implemented")
    {{#if function.stream?}}

    @staticmethod
    def createPublisher_{{function.name}}(callback=None):
        cdef unique_ptr[pair[cServerStream[{{#partial types.cython_cpp_type type=function.stream.elem_type}}], {{!
        }}cServerStreamPublisher[{{#partial types.cython_cpp_type type=function.stream.elem_type}}]]] streams = {{!
        }}make_unique[pair[cServerStream[{{#partial types.cython_cpp_type type=function.stream.elem_type}}], {{!
        }}cServerStreamPublisher[{{#partial types.cython_cpp_type type=function.stream.elem_type}}]]]({{!
        }}cServerStream[{{#partial types.cython_cpp_type type=function.stream.elem_type}}].createPublisher(pythonFuncToCppFunc(callback)))

        return (ServerStream_{{#partial services.cython_cpp_type_ident type=function.stream.elem_type}}._fbthrift_create(cmove(deref(streams).first)), {{!
        }}ServerPublisher_{{#partial services.cython_cpp_type_ident type=function.stream.elem_type}}._fbthrift_create(cmove(deref(streams).second)))
    {{/if function.stream?}}
{{/each}}

    @staticmethod
    def __get_metadata__():
        cdef __fbthrift_cThriftServiceMetadataResponse response
        ServiceMetadata[_fbthrift_services_interface.c{{service.cpp_name}}SvIf].gen(response)
        return __MetadataBox.box(cmove(deref(response.metadata())))

    @staticmethod
    def __get_thrift_name__():
        return "{{program.name}}.{{service.name}}"

{{/each}}

{{> services/callback}}
{{/if (not program.auto_migrate?)}}
