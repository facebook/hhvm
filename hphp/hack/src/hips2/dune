(library
 (name hips2)
 (wrapped true) ; only hips2.mli is visible
 (flags
  (:standard -linkall))
 (libraries
  core_kernel
  utils_core
  sys_utils)
  (preprocess
  (pps visitors.ppx ppx_deriving.std ppx_hash ppx_sexp_conv)))
