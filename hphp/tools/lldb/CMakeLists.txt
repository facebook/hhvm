GET_DIRECTORY_PROPERTY(SOURCE_INCLUDE_DIRS INCLUDE_DIRECTORIES)
GET_DIRECTORY_PROPERTY(HHVM_DEFINITIONS COMPILE_DEFINITIONS)

configure_file(hhvm-lldb.in hhvm-lldb ESCAPE_QUOTES @ONLY)
install(PROGRAMS ${CMAKE_CURRENT_BINARY_DIR}/hhvm-lldb
    DESTINATION bin
    COMPONENT dev)

file(GLOB LLDB_SCRIPTS
    RELATIVE ${CMAKE_CURRENT_SOURCE_DIR}
    FOLLOW_SYMLINKS *.py)

install(FILES ${LLDB_SCRIPTS}
    DESTINATION "${CMAKE_INSTALL_LIBDIR}/hhvm/lldb"
    COMPONENT dev)
