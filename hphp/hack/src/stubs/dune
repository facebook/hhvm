(library
  (name ai_stubs)
  (wrapped false)
  (modules
    ai
    ai_options)
  (libraries
    errors))

(library
  (name build)
  (wrapped false)
  (modules
    buildMain))

(library
  (name debug_stubs)
  (wrapped false)
  (modules
    debug
    sof)
  (libraries
    parser_sigs))

(library
  (name hackc_parse_delegator)
  (wrapped false)
  (modules
    hackc_parse_delegator))

(library
  (name linting_stubs)
  (wrapped false)
  (modules
    linting_service))

(library
  (name load_script_stubs)
  (wrapped false)
  (modules
    load_script
    loadScriptConfig
    loadScriptUtils))

(library
  (name messages_stubs)
  (wrapped false)
  (modules
    clientMessages))

(library
  (name state_loader)
  (wrapped false)
  (modules
    state_loader
    xdb)
  (libraries
    hg
    server_monitor))
