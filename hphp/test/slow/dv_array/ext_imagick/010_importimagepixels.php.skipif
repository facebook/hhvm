<?hh

<<__EntryPoint>>
function main() {
  if (!extension_loaded("imagick")) die("skip no imagick extension");
  if (!method_exists("imagick", "importImagePixels")) {
    die("skip imagick::importImagePixels not available");
  }
}
