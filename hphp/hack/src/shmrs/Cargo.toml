# @generated by autocargo from //hphp/hack/src/shmrs:shmrs
[package]
name = "shmrs"
version = "0.0.0"
edition = "2018"

[lib]
path = "mod.rs"

[dependencies]
hashbrown = { version = "0.11.2", features = ["nightly", "raw", "serde"] }
libc = "0.2.98"
static_assertions = "1.1.0"

[dev-dependencies]
nix = "0.20"
rand = { version = "0.8", features = ["small_rng"] }

[profile.release]
incremental = false

[profile.dev]
incremental = false

[profile.test]
incremental = false

[profile.bench]
incremental = false
