{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}{{!

Python wrappers of the structs defined in the services files. This file is
compiled into it's own module to be included by clients and services and
end-user code. It's one of the more complicated files, as it has to map
Pythonic APIs to C++ objects and back.

One of the nastier things in this file is the definition of containers.
A separate container wrapper has to be defined for each type of contained
attribute because Cython can't template C++ classes. So, for example, we need
a List__int16 or a List__string or a Map__string_mystruct instance for each
container/type combination. Consider that containers can contain other containers
or structs that contain containers and you realize how messy this can get.
Further, we'd prefer to have the end user freed from having to know about these
container types, so we'll need to define factories for them based on what they
want to include.

}}
{{#if type.list?}}
class {{type.flat_name}}(thrift.py3.types.List):
    {{! make the type appear to originate from .types module }}
    __module__ = _fbthrift__module_name__
    {{! __slots__ prevents accidental treatment as py-deprecated }}
    __slots__ = ()

    def __init__(self, items=None, private_ctor_token=None) -> None:
        if private_ctor_token is thrift.py3.types._fbthrift_list_private_ctor:
            {{!
                For internal use only, when items list has been created internally
                and each item has been converted from cpp so we can trust the type.
                Do not use with user-exposed `list` to avoid accidental mutability.
            }}
            _py_obj = items
        elif isinstance(items, {{type.flat_name}}):
            _py_obj = list(items)
        elif items is None:
            _py_obj = []
        else:
  {{#if type.containerOfString?}}
            if isinstance(items, str):
                raise TypeError("If you really want to pass a string into a {{#partial pep484.type_name type=type}} field, explicitly convert it first.")
  {{/if type.containerOfString?}}
            check_method = {{type.flat_name}}._check_item_type_or_raise
            _py_obj = [check_method(item) for item in items]

        super().__init__(_py_obj, {{type.flat_name}})

    @staticmethod
    def _check_item_type_or_raise(item):
  {{#let elem_type=type.true_type.elem_type}}
  {{#if elem_type.container?}}
        if item is None:
            raise TypeError("None is not of the type {{#partial pep484.type_name type=elem_type}}")
        if not isinstance(item, {{#partial types.python_type type=elem_type}}):
            item = {{#partial types.python_type type=elem_type}}(item)
  {{#else}}
        if not (
            isinstance(item, {{#partial types.python_is_instance_type type=elem_type}}){{#if elem_type.enum?}} or
            isinstance(item, thrift.py3.types.BadEnum){{/if elem_type.enum?}}
        ):
            raise TypeError(f"{item!r} is not of type {{#partial pep484.type_name type=elem_type}}")
  {{/if elem_type.container?}}
        return item

    {{! the __contains__, index, and count APIs don't raise TypeError}}
    @staticmethod
    def _check_item_type_or_none(item):
        if item is None:
            return None
        if isinstance(item, {{#partial types.python_type type=elem_type}}):
            return item
  {{#if elem_type.container?}}
        try:
            return {{#partial types.python_type type=elem_type}}(item)
        except:
            pass
  {{/if elem_type.container?}}

    @staticmethod
    def __get_reflection__():
        return get_types_reflection().get_reflection__{{type.flat_name}}()

  {{#if program.inplace_migrate?}}
    @staticmethod
    def from_python(python_list: thrift.python.types.List) -> {{type.flat_name}}:
    {{#if (not elem_type.needs_convert?)}}
        _items = list(python_list)
    {{#else}}
        _items = [
            {{#partial types.python_type type=elem_type}}.from_python(item)
            for item in python_list
        ]
    {{/if (not elem_type.needs_convert?)}}
        return {{type.flat_name}}(
            items=_items,
            private_ctor_token=thrift.py3.types._fbthrift_list_private_ctor,
        )

  {{#else if type.is_container_of_struct?}}
    @staticmethod
    def from_python(python_list: thrift.python.types.List) -> "{{type.flat_name}}":
        _items = [
    {{#if elem_type.is_container_of_struct?}}
            {{#partial types.python_type type=elem_type}}.from_python(item)
    {{#else}}
            _ensure_py3_or_raise(item, "item", {{#partial types.python_is_instance_type type=elem_type}})
    {{/if elem_type.is_container_of_struct?}}
            for item in python_list
        ]
        return {{type.flat_name}}(
            items=_items,
            private_ctor_token=thrift.py3.types._fbthrift_list_private_ctor,
        )
  {{/if program.inplace_migrate?}}

Sequence.register({{type.flat_name}})

{{#else if type.set?}}
class {{type.flat_name}}(thrift.py3.types.Set):
    {{! make the type appear to originate from .types module }}
    __module__ = _fbthrift__module_name__
    {{! __slots__ prevents accidental treatment as py-deprecated }}
    __slots__ = ()

    def __init__(self, items=None, private_ctor_token=None) -> None:
        if private_ctor_token is thrift.py3.types._fbthrift_set_private_ctor:
            {{!
                For internal use only, when items set has been created internally
                and each item has been converted from cpp so we can trust the type.
                Do not use with user-exposed `set` to avoid accidental mutability.
            }}
            _py_obj = items
        elif isinstance(items, {{type.flat_name}}):
            _py_obj = frozenset(items)
        elif items is None:
            _py_obj = frozenset()
        else:
  {{#if type.containerOfString?}}
            if isinstance(items, str):
                raise TypeError("If you really want to pass a string into a {{#partial pep484.type_name type=type}} field, explicitly convert it first.")
  {{/if type.containerOfString?}}
            check_method = {{type.flat_name}}._check_item_type_or_raise
            _py_obj = frozenset(check_method(item) for item in items)

        super().__init__(_py_obj, {{type.flat_name}})

    @staticmethod
    def _check_item_type_or_raise(item):
  {{#let elem_type=type.true_type.elem_type}}
  {{#if elem_type.container?}}
        if item is None:
            raise TypeError("None is not of the type {{#partial pep484.type_name type=elem_type}}")
        if not isinstance(item, {{#partial types.python_type type=elem_type}}):
            item = {{#partial types.python_type type=elem_type}}(item)
  {{#else}}
        if not (
            isinstance(item, {{#partial types.python_is_instance_type type=elem_type}}){{#if elem_type.enum?}} or
            isinstance(item, thrift.py3.types.BadEnum){{/if elem_type.enum?}}
        ):
            raise TypeError(f"{item!r} is not of type {{#partial pep484.type_name type=elem_type}}")
  {{/if elem_type.container?}}
        return item

    {{! __contains__ doesn't raise TypeError}}
    @staticmethod
    def _check_item_type_or_none(item):
        if item is None:
            return None
        if isinstance(item, {{#partial types.python_type type=elem_type}}):
            return item
  {{#if elem_type.container?}}
        try:
            return {{#partial types.python_type type=elem_type}}(item)
        except:
            return None
  {{/if elem_type.container?}}

    @staticmethod
    def __get_reflection__():
        return get_types_reflection().get_reflection__{{type.flat_name}}()

  {{#if program.inplace_migrate?}}
    @staticmethod
    def from_python(python_set: thrift.python.types.Set) -> {{type.flat_name}}:
    {{#if (not elem_type.needs_convert?)}}
        _items = frozenset(python_set)
    {{#else}}
        _items = frozenset(
            {{#partial types.python_type type=elem_type}}.from_python(item)
            for item in python_set
        )
    {{/if (not elem_type.needs_convert?)}}
        return {{type.flat_name}}(
            items=_items,
            private_ctor_token=thrift.py3.types._fbthrift_set_private_ctor,
        )

  {{#else if type.is_container_of_struct?}}
    @staticmethod
    def from_python(python_set: thrift.python.types.Set) -> "{{type.flat_name}}":
        _items = frozenset(
    {{#if elem_type.is_container_of_struct?}}
            {{#partial types.python_type type=elem_type}}.from_python(item)
    {{#else}}
            _ensure_py3_or_raise(item, "item", {{#partial types.python_is_instance_type type=elem_type}})
    {{/if elem_type.is_container_of_struct?}}
            for item in python_set
        )
        return {{type.flat_name}}(
            items=_items,
            private_ctor_token=thrift.py3.types._fbthrift_set_private_ctor,
        )
  {{/if program.inplace_migrate?}}

Set.register({{type.flat_name}})

{{#else if type.map?}}
class {{type.flat_name}}(thrift.py3.types.Map):
    {{! make the type appear to originate from .types module }}
    __module__ = _fbthrift__module_name__
    {{! __slots__ prevents accidental treatment as py-deprecated }}
    __slots__ = ()

    _FBTHRIFT_USE_SORTED_REPR = {{!
        }}{{#if type.defaultTemplate?}}True{{#else}}False{{/if}}

    def __init__(self, items=None, private_ctor_token=None) -> None:
        if private_ctor_token is thrift.py3.types._fbthrift_map_private_ctor:
            {{!
                For internal use only, when items dict has been created internally
                and each item has been converted from cpp so we can trust the type.
                Do not use with user-exposed `dict` to avoid accidental mutability.
            }}
            _py_obj = items
        elif isinstance(items, {{type.flat_name}}):
            _py_obj = dict(items)
        elif items is None:
            _py_obj = dict()
        else:
            check_key = {{type.flat_name}}._check_key_type_or_raise
            check_val = {{type.flat_name}}._check_val_type_or_raise
            _py_obj = {check_key(k) : check_val(v) for k, v in items.items()}

        super().__init__(_py_obj, {{type.flat_name}})

    @staticmethod
    def _check_key_type_or_raise(key):
  {{#let key_type=type.true_type.key_type}}
  {{#if key_type.container?}}
        if key is None:
            raise TypeError("None is not of the type {{#partial pep484.type_name type=key_type}}")
        if not isinstance(key, {{#partial types.python_type type=key_type}}):
            key = {{#partial types.python_type type=key_type}}(key)
  {{#else}}
        if not (
            isinstance(key, {{#partial types.python_is_instance_type type=key_type}}){{#if key_type.enum?}} or
            isinstance(key, thrift.py3.types.BadEnum){{/if key_type.enum?}}
        ):
            raise TypeError(f"{key!r} is not of type {{#partial pep484.type_name type=key_type}}")
  {{/if key_type.container?}}
        return key

    {{! __contains__ doesn't raise TypeError; __getitem__ uses KeyError }}
    @staticmethod
    def _check_key_type_or_none(key):
        if key is None:
            return None
        if isinstance(key, {{#partial types.python_type type=key_type}}):
            return key
  {{#if key_type.container?}}
        try:
            return {{#partial types.python_type type=key_type}}(key)
        except:
            return None
  {{/if key_type.container?}}

    @staticmethod
    def _check_val_type_or_raise(item):
  {{#let val_type=type.true_type.val_type}}
  {{#if val_type.container?}}
        if item is None:
            raise TypeError("None is not of the type {{#partial pep484.type_name type=val_type}}")
        if not isinstance(item, {{#partial types.python_type type=val_type}}):
            item = {{#partial types.python_type type=val_type}}(item)
  {{#else}}
        if not (
            isinstance(item, {{#partial types.python_is_instance_type type=val_type}}){{#if val_type.enum?}} or
            isinstance(item, thrift.py3.types.BadEnum){{/if val_type.enum?}}
        ):
            raise TypeError(f"{item!r} is not of type {{#partial pep484.type_name type=val_type}}")
  {{/if val_type.container?}}
        return item

    @staticmethod
    def __get_reflection__():
        return get_types_reflection().get_reflection__{{type.flat_name}}()

  {{#if program.inplace_migrate?}}
    @staticmethod
    def from_python(python_map: thrift.python.types.Map) -> {{type.flat_name}}:
    {{#if (not key_type.needs_convert?)}}
        _keys = python_map.keys()
    {{#else}}
        _keys = (
            {{#partial types.python_type type=key_type}}.from_python(key)
            for key in python_map.keys()
        )
    {{/if (not key_type.needs_convert?)}}
    {{#if (not val_type.needs_convert?)}}
        _values = python_map.values()
    {{#else}}
        _values = (
            {{#partial types.python_type type=val_type}}.from_python(item)
            for item in python_map.values()
        )
    {{/if (not val_type.needs_convert?)}}
        return {{type.flat_name}}(
            items=dict(zip(_keys, _values)),
            private_ctor_token=thrift.py3.types._fbthrift_map_private_ctor,
        )

  {{#else if type.is_container_of_struct?}}
    @staticmethod
    def from_python(python_map: thrift.python.types.Map) -> "{{type.flat_name}}":
    {{#if key_type.is_container_of_struct?}}
        _keys = (
            {{#partial types.python_type type=key_type}}.from_python(key)
            for key in python_map.keys()
        )
    {{#else if key_type.structured?}}
        _keys = (
            _ensure_py3_or_raise(key, "key", {{#partial types.python_is_instance_type type=key_type}})
            for key in python_map.keys()
        )
    {{#else}}
        _keys = python_map.keys()
    {{/if key_type.is_container_of_struct?}}
    {{#if val_type.is_container_of_struct?}}
        _values = (
            {{#partial types.python_type type=val_type}}.from_python(value)
            for value in python_map.values()
        )
    {{#else if val_type.structured?}}
        _values = (
            _ensure_py3_or_raise(value, "value", {{#partial types.python_is_instance_type type=val_type}})
            for value in python_map.values()
        )
    {{#else}}
        _values = python_map.values()
    {{/if val_type.is_container_of_struct?}}
        return {{type.flat_name}}(
            items=dict(zip(_keys, _values)),
            private_ctor_token=thrift.py3.types._fbthrift_map_private_ctor,
        )
  {{/if program.inplace_migrate?}}

Mapping.register({{type.flat_name}})
{{/if type.list?}}
