<?hh
/**  The default module. Code that isn't in any module explicitly is considered to be part of this module.
  This module can be globbed by PACKAGES.toml, but you can't write `module default;` explicitly in any code.
**/
new module default {}
