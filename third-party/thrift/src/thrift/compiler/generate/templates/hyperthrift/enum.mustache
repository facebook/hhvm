{{> generated}}

package {{packageName}};

import androidx.annotation.IntDef;
import java.util.HashMap;
import javax.annotation.Nullable;

@IntDef({
{{#enum:values}}
  {{enum:javaCapitalName}}.{{enum_value:javaConstantName}},
{{/enum:values}}
})
public @interface {{enum:javaCapitalName}} {
{{#enum:values}}
  int {{enum_value:javaConstantName}} = {{enum_value:value}};
{{/enum:values}}

  public static final class Lookup {
    public static String toString(@Nullable Integer val, String defaultValue) {
      if (val == null) { return defaultValue; }

      switch (val) {
      {{#enum:values}}
        case ({{enum_value:javaConstantName}}): return "{{enum_value:javaConstantName}}";
      {{/enum:values}}
      }

      return defaultValue;
    }
  }

}
