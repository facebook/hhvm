# testing nativeimpl dealt with properly
# just semdiffing this with itself for now
# /:systemlib.phpzlib starts here


.filepath "/:systemlib.phpzlib";

.hh_file 1;

.main (1,1) {
  Int 0
  RetC
}

.function [unique builtin persistent skip_frame foldable "__HipHopSpecific"("""a:0:{}""") "__IsFoldable"("""a:0:{}""") "__Native"("""a:0:{}""")] (353,353) <"HH\\mixed" N  > nzuncompress(<"HH\\string" "HH\\string" hh_type > $compressed) {
  NativeImpl
}

# /:systemlib.phpzlib ends here
