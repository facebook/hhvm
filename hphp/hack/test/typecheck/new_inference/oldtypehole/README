Examples of unsoundness
This directory contains files that should be rejected by the type-checker but prior to constraint-based inference were rejected only because of nasty hacks (e.g. using the "todo list")
