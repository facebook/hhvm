warning: Lint[5656] This is a cross-boundary edge from package pkg1 into package pkg2. Consider one of the following:
  (1) if this value isn't used to load the class via new $cls(), $cls::meth(), etc, change this to a nameof expression;
  (2) use __RequirePackage (if the function should only be invoked from pkg2);
  (3) gate this reference via a package check (if this portion of the function is meant to be gated by an environment check).
Alternatively, move the callee into the caller's package via sl mv.
See https://docs.hhvm.com/hack/packages/cross-package-calls for examples.
You may also consider adding <<file: __PackageOverride('pkg1')>> to the target file to include it in the pkg1 package for safety. [1]

lint_classptr_07.php--ptr7b.php:10:10
     8 | class SourceClass7 {
     9 |   public function test(): void {
[1] 10 |     $c = TargetClass7::class; // should raise lint 5656
    11 |   }
    12 | }


