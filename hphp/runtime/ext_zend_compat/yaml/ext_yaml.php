<?hh
// @generated by idl-to-hni.php

/* Generate a YAML representation of the provided data in the filename.
 */
<<__Native("ZendCompat")>>
function yaml_emit_file(string $filename,
                        mixed $data,
                        int $encoding,
                        int $linebreak,
                        ?array $callbacks = null): mixed;

/* Generate a YAML representation of the provided data.
 */
<<__Native("ZendCompat")>>
function yaml_emit(mixed $data,
                   int $encoding,
                   int $linebreak,
                   ?array $callbacks = null): mixed;

/* Convert all or part of a YAML document stream read from a file to a PHP
 * variable.
 */
<<__Native("ZendCompat")>>
function yaml_parse_file(string $filename,
                         int $pos = 0,
                         mixed &$ndocs = null,
                         ?array $callbacks = null): mixed;

/* Convert all or part of a YAML document stream read from a URL to a PHP
 * variable.
 */
<<__Native("ZendCompat")>>
function yaml_parse_url(string $url,
                        int $pos = 0,
                        mixed &$ndocs = null,
                        ?array $callbacks = null): mixed;

/* Convert all or part of a YAML document stream to a PHP variable.
 */
<<__Native("ZendCompat")>>
function yaml_parse(string $input,
                    int $pos = 0,
                    mixed &$ndocs = null,
                    ?array $callbacks = null): mixed;
