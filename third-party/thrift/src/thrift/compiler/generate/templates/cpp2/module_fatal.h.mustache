{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}
{{#let partial fatal_strings |items|}}
  {{#each items as |item|}}
using {{item}} = __fbthrift_strings::{{item}};
  {{/each}}
{{/let partial}}
{{> Autogen}}
#pragma once

#include <thrift/lib/cpp2/reflection/reflection.h>

#include <fatal/type/list.h>
#include <fatal/type/pair.h>
#include <fatal/type/sequence.h>

#include "{{program:include_prefix}}{{program:name}}_types.h"
namespace {{program:cpp_qualified_namespace}} {

namespace __fbthrift_refl {

struct __fbthrift_strings_{{program:name}} {
  {{#each (map.items program:fatal_identifiers) as |item|}}
  using {{item.key}} = {{item.value}};
  {{/each}}
};

} // __fbthrift_refl

class {{program:name}}_tags {
  using __fbthrift_strings = __fbthrift_refl::__fbthrift_strings_{{program:name}};
  struct __fbthrift_languages {
    {{!
      BY DESIGN: Not using the `fatal_strings` partial to maintain whitespace parity,
      as we're relying on byte-for-byte identical codegen as a test for Whisker migrations.
      TODO(T256504524): Use the same partial for all of these, consistent whitespace behavior
    }}
    {{#each program:fatal_languages as |name|}}
    using {{name}} = __fbthrift_strings::{{name}};
    {{/each}}
  };
  struct __fbthrift_enums {
    {{#partial fatal_strings items=program:fatal_enums}}
  };
  struct __fbthrift_unions {
    {{#partial fatal_strings items=program:fatal_unions}}
  };
  struct __fbthrift_structs {
    {{#partial fatal_strings items=program:fatal_structs}}
  };
  struct __fbthrift_constants {
    {{#partial fatal_strings items=program:fatal_constants}}
  };
  struct __fbthrift_services {
    {{#partial fatal_strings items=program:fatal_services}}
  };
 public:
  struct module {};

  using strings = __fbthrift_strings;
  using languages = __fbthrift_languages;
  using enums = __fbthrift_enums;
  using unions = __fbthrift_unions;
  using structs = __fbthrift_structs;
  using constants = __fbthrift_constants;
  using services = __fbthrift_services;
};

} // namespace {{program:cpp_qualified_namespace}}
