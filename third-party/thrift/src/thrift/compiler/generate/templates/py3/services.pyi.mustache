{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}{{!

This template should always mirror services.pyx but for mypy typing

}}
{{#import "common/pep484" as pep484}}
{{#import "common/services" as services}}
{{#import "common/types" as types}}
{{> common/auto_generated_py}}

import folly.iobuf as _fbthrift_iobuf
import typing as _typing
from thrift.py3.server import RequestContext, ServiceInterface
from abc import abstractmethod, ABCMeta

import {{program.module_path}}.types as {{program.module_path_for_alias}}_types
{{#each program.includeNamespaces as |include|}}
  {{#if include.import_services?}}
import {{include.program.module_path}}.services as {{include.program.module_path_for_alias}}_services
  {{/if include.import_services?}}
  {{#if include.program.has_types?}}
import {{include.program.module_path}}.types as {{include.program.module_path_for_alias}}_types
  {{/if include.program.has_types?}}
{{/each}}

{{#each program.services as |service|}}
_{{service.name}}InterfaceT = _typing.TypeVar('_{{service.name}}InterfaceT', bound='{{service.name}}Interface')


class {{service.name}}Interface(
    {{#let parent = service.extends}}{{#if (object.notnull? parent)}}{{#if parent.externalProgram?}}
    {{parent.program.module_path_for_alias}}_services.{{/if parent.externalProgram?}}{{parent.name}}Interface,
    {{#else}}
    ServiceInterface,
    {{/if (object.notnull? parent)}}
    metaclass=ABCMeta,
):
  {{#each service.supportedFunctions as |function|}}

    {{#if function.stream?}}
    @staticmethod
    def createPublisher_{{function.name}}(callback=None) -> _typing.Tuple[
        _typing.AsyncGenerator[{{#partial pep484.type_name type=function.stream.elem_type}}, None],
        {{program.module_path_for_alias}}_types.ServerPublisher_{{#partial services.cython_cpp_type_ident type=function.stream.elem_type}}
    ]: ...

    {{/if function.stream?}}

    @abstractmethod
    {{#if (not function.stream?)}}async {{/if}}{{#if function.stream_has_first_response?}}async {{/if}}def {{function.name}}(
        self{{#each function.params.fields as |field|}},
        {{field.py_name}}: {{#partial pep484.type_name type=field.type}}{{/each}}
    ) -> {{#partial pep484.return_type function=function}}: ...
  {{/each}}
    pass


{{/each}}
