// @generated by Thrift for thrift/compiler/test/fixtures/exceptions/src/module.thrift
// This file is auto-generated. Do not edit manually.
//
// Copyright (c) Meta Platforms, Inc. and affiliates.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
import Foundation
import FBThrift

/// Auto-generated from Fiery
public struct Fiery: ThriftSerializable, Hashable, LocalizedError {
    public var message: String = ""

    /// The field designated by @thrift.ExceptionMessage, surfaced as the Error's
    /// localized description (guide 2.5.32).
    public var errorDescription: String? { self.message }

    public init() {}

    /// Resets every field to its intrinsic (standard) default, per the Object
    /// Model `clear()` (guide 2.1.22). Unlike `init()`, this ignores custom
    /// IDL defaults: optional fields become nil, others their type's zero value.
    public mutating func clear() {
        self.message = ""
    }

    public func write<W: ProtocolWriter>(to writer: W) {
        writer.writeFieldBegin(.string, 1)
        self.message.thriftWrite(to: writer)
        writer.writeFieldStop()
    }

    public init<R: ProtocolReader>(from reader: R) throws {
        self.init()
        while true {
            let (fieldType, fieldId) = try reader.readFieldBegin()
            if fieldType == .stop {
                break
            }
            switch fieldId {
            case 1:
                if fieldType == .string {
                    self.message = try String.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            default:
                try reader.skip(fieldType)
            }
        }
    }
}

/// Auto-generated from Serious
public struct Serious: ThriftSerializable, Hashable, LocalizedError {
    public var sonnet: String?

    /// The field designated by @thrift.ExceptionMessage, surfaced as the Error's
    /// localized description (guide 2.5.32).
    public var errorDescription: String? { self.sonnet }

    public init() {}

    /// Resets every field to its intrinsic (standard) default, per the Object
    /// Model `clear()` (guide 2.1.22). Unlike `init()`, this ignores custom
    /// IDL defaults: optional fields become nil, others their type's zero value.
    public mutating func clear() {
        self.sonnet = nil
    }

    public func write<W: ProtocolWriter>(to writer: W) {
        if let value = self.sonnet {
            writer.writeFieldBegin(.string, 1)
            value.thriftWrite(to: writer)
        }
        writer.writeFieldStop()
    }

    public init<R: ProtocolReader>(from reader: R) throws {
        self.init()
        while true {
            let (fieldType, fieldId) = try reader.readFieldBegin()
            if fieldType == .stop {
                break
            }
            switch fieldId {
            case 1:
                if fieldType == .string {
                    self.sonnet = try String.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            default:
                try reader.skip(fieldType)
            }
        }
    }
}

/// Auto-generated from ComplexFieldNames
public struct ComplexFieldNames: ThriftSerializable, Hashable, LocalizedError {
    public var error_message: String = ""
    public var internal_error_message: String = ""

    /// The field designated by @thrift.ExceptionMessage, surfaced as the Error's
    /// localized description (guide 2.5.32).
    public var errorDescription: String? { self.internal_error_message }

    public init() {}

    /// Resets every field to its intrinsic (standard) default, per the Object
    /// Model `clear()` (guide 2.1.22). Unlike `init()`, this ignores custom
    /// IDL defaults: optional fields become nil, others their type's zero value.
    public mutating func clear() {
        self.error_message = ""
        self.internal_error_message = ""
    }

    public func write<W: ProtocolWriter>(to writer: W) {
        writer.writeFieldBegin(.string, 1)
        self.error_message.thriftWrite(to: writer)
        writer.writeFieldBegin(.string, 2)
        self.internal_error_message.thriftWrite(to: writer)
        writer.writeFieldStop()
    }

    public init<R: ProtocolReader>(from reader: R) throws {
        self.init()
        while true {
            let (fieldType, fieldId) = try reader.readFieldBegin()
            if fieldType == .stop {
                break
            }
            switch fieldId {
            case 1:
                if fieldType == .string {
                    self.error_message = try String.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            case 2:
                if fieldType == .string {
                    self.internal_error_message = try String.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            default:
                try reader.skip(fieldType)
            }
        }
    }
}

/// Auto-generated from CustomFieldNames
public struct CustomFieldNames: ThriftSerializable, Hashable, LocalizedError {
    public var error_message: String = ""
    public var internal_error_message: String = ""

    /// The field designated by @thrift.ExceptionMessage, surfaced as the Error's
    /// localized description (guide 2.5.32).
    public var errorDescription: String? { self.internal_error_message }

    public init() {}

    /// Resets every field to its intrinsic (standard) default, per the Object
    /// Model `clear()` (guide 2.1.22). Unlike `init()`, this ignores custom
    /// IDL defaults: optional fields become nil, others their type's zero value.
    public mutating func clear() {
        self.error_message = ""
        self.internal_error_message = ""
    }

    public func write<W: ProtocolWriter>(to writer: W) {
        writer.writeFieldBegin(.string, 1)
        self.error_message.thriftWrite(to: writer)
        writer.writeFieldBegin(.string, 2)
        self.internal_error_message.thriftWrite(to: writer)
        writer.writeFieldStop()
    }

    public init<R: ProtocolReader>(from reader: R) throws {
        self.init()
        while true {
            let (fieldType, fieldId) = try reader.readFieldBegin()
            if fieldType == .stop {
                break
            }
            switch fieldId {
            case 1:
                if fieldType == .string {
                    self.error_message = try String.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            case 2:
                if fieldType == .string {
                    self.internal_error_message = try String.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            default:
                try reader.skip(fieldType)
            }
        }
    }
}

/// Auto-generated from ExceptionWithPrimitiveField
public struct ExceptionWithPrimitiveField: ThriftSerializable, Hashable, LocalizedError {
    public var message: String = ""
    public var error_code: Int32 = 0

    /// The field designated by @thrift.ExceptionMessage, surfaced as the Error's
    /// localized description (guide 2.5.32).
    public var errorDescription: String? { self.message }

    public init() {}

    /// Resets every field to its intrinsic (standard) default, per the Object
    /// Model `clear()` (guide 2.1.22). Unlike `init()`, this ignores custom
    /// IDL defaults: optional fields become nil, others their type's zero value.
    public mutating func clear() {
        self.message = ""
        self.error_code = 0
    }

    public func write<W: ProtocolWriter>(to writer: W) {
        writer.writeFieldBegin(.string, 1)
        self.message.thriftWrite(to: writer)
        writer.writeFieldBegin(.i32, 2)
        self.error_code.thriftWrite(to: writer)
        writer.writeFieldStop()
    }

    public init<R: ProtocolReader>(from reader: R) throws {
        self.init()
        while true {
            let (fieldType, fieldId) = try reader.readFieldBegin()
            if fieldType == .stop {
                break
            }
            switch fieldId {
            case 1:
                if fieldType == .string {
                    self.message = try String.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            case 2:
                if fieldType == .i32 {
                    self.error_code = try Int32.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            default:
                try reader.skip(fieldType)
            }
        }
    }
}

/// Auto-generated from ExceptionWithStructuredAnnotation
public struct ExceptionWithStructuredAnnotation: ThriftSerializable, Hashable, LocalizedError {
    public var message_field: String = ""
    public var error_code: Int32 = 0

    /// The field designated by @thrift.ExceptionMessage, surfaced as the Error's
    /// localized description (guide 2.5.32).
    public var errorDescription: String? { self.message_field }

    public init() {}

    /// Resets every field to its intrinsic (standard) default, per the Object
    /// Model `clear()` (guide 2.1.22). Unlike `init()`, this ignores custom
    /// IDL defaults: optional fields become nil, others their type's zero value.
    public mutating func clear() {
        self.message_field = ""
        self.error_code = 0
    }

    public func write<W: ProtocolWriter>(to writer: W) {
        writer.writeFieldBegin(.string, 1)
        self.message_field.thriftWrite(to: writer)
        writer.writeFieldBegin(.i32, 2)
        self.error_code.thriftWrite(to: writer)
        writer.writeFieldStop()
    }

    public init<R: ProtocolReader>(from reader: R) throws {
        self.init()
        while true {
            let (fieldType, fieldId) = try reader.readFieldBegin()
            if fieldType == .stop {
                break
            }
            switch fieldId {
            case 1:
                if fieldType == .string {
                    self.message_field = try String.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            case 2:
                if fieldType == .i32 {
                    self.error_code = try Int32.thriftRead(from: reader)
                } else {
                    try reader.skip(fieldType)
                }
            default:
                try reader.skip(fieldType)
            }
        }
    }
}

/// Auto-generated from Banal
public struct Banal: ThriftSerializable, Hashable, Error {

    public init() {}

    /// Resets every field to its intrinsic (standard) default, per the Object
    /// Model `clear()` (guide 2.1.22). Unlike `init()`, this ignores custom
    /// IDL defaults: optional fields become nil, others their type's zero value.
    public mutating func clear() {
    }

    public func write<W: ProtocolWriter>(to writer: W) {
        writer.writeFieldStop()
    }

    public init<R: ProtocolReader>(from reader: R) throws {
        self.init()
        while true {
            let (fieldType, fieldId) = try reader.readFieldBegin()
            if fieldType == .stop {
                break
            }
            switch fieldId {
            default:
                try reader.skip(fieldType)
            }
        }
    }
}

