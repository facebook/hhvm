<?xml version="1.0" encoding="UTF-8"?>
<!-- vim: set ts=4 sw=4 et: -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.facebook</groupId>
        <artifactId>facebook-base-pom</artifactId>
        <version>11</version>
    </parent>

    <groupId>com.facebook.mojo</groupId>
    <artifactId>fbthrift-maven-plugin-root</artifactId>
    <packaging>pom</packaging>
    <version>2.2-SNAPSHOT</version>

    <modules>
        <module>fbthrift-maven-plugin</module>
        <module>fbthrift-maven-plugin-example</module>
    </modules>

    <name>${project.artifactId}</name>

    <properties>
        <project.build.targetJdk>1.8</project.build.targetJdk>
    </properties>

    <distributionManagement>
        <!--
            We still push our jars to the old location. We can't push to the new
            location (namely
            https://maven.thefacebook.com/nexus/content/repositories/releases/)
            because the new location does not allow pushing jars in groupId's in
            org/apache.
        -->
        <repository>
            <id>libs-releases-local</id>
            <url>https://maven.thefacebook.com/nexus/content/repositories/libs-releases-local/</url>
        </repository>
        <snapshotRepository>
            <id>libs-snapshots-local</id>
            <url>https://maven.thefacebook.com/nexus/content/repositories/libs-snapshots-local/</url>
        </snapshotRepository>
    </distributionManagement>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.facebook.mojo</groupId>
                <artifactId>fbthrift-maven-plugin</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.facebook.mojo</groupId>
                <artifactId>fbthrift-example-project</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>4.5.3</version>
            </plugin>
            <!-- findbugs-maven-plugin has been deprecated -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>findbugs-maven-plugin</artifactId>
                <version>3.0.5</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
