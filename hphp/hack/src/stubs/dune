(library
  (name ai_stubs)
  (wrapped false)
  (modules
    ai
    ai_options)
  (libraries
    errors))

(library
  (name build)
  (wrapped false)
  (modules
    buildMain))

(library
  (name debug_stubs)
  (wrapped false)
  (modules
    debug
    sof)
  (libraries
    full_fidelity))

(library
  (name rust_ffi_stubs)
  (wrapped false)
  (modules ))

(library
  (name linting_stubs)
  (wrapped false)
  (modules
    linting_service))

(library
  (name load_script_stubs)
  (wrapped false)
  (modules
    load_script
    loadScriptConfig
    loadScriptUtils))

(library
  (name lru_worker_stubs)
  (wrapped false)
  (modules
    lru_worker))

(library
  (name messages_stubs)
  (wrapped false)
  (modules
    clientMessages))

(library
  (name vfs_stubs)
  (wrapped false)
  (modules
    vfs)
  (libraries
    relative_path))

(library
  (name remote_scheduler_stubs)
  (wrapped false)
  (modules
    remoteScheduler)
  (libraries
    file_info
    naming
    procs_procs
    relative_path
    sys_utils))

(library
  (name remote_worker_stubs)
  (wrapped false)
  (modules
    remoteWorker)
  (libraries
    errors
    file_info
    relative_path
    sys_utils))

(library
  (name state_loader_stubs)
  (wrapped false)
  (modules
    state_loader
    saved_state_loader
    state_loader_futures
    state_loader_lwt
    xdb)
  (libraries
    hg
    server_monitor))

(library
  (name shared_lru_stubs)
  (wrapped false)
  (modules
    shared_lru))
