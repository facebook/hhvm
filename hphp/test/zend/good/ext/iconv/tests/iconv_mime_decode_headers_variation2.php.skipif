<?hh
<<__EntryPoint>> function main(): void {
extension_loaded('iconv') || die('skip');
function_exists('iconv_mime_decode_headers') || die("skip iconv_mime_decode_headers() is not available in this build");
}
