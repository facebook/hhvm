// Autogenerated by Thrift for thrift/compiler/test/fixtures/bidi/src/module.thrift
//
// DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
//  @generated

package module

import (
    "context"
    "errors"
    "fmt"
    "io"
    "iter"
    "reflect"
    "sync"

    thrift "github.com/facebook/fbthrift/thrift/lib/go/thrift/types"
    metadata "github.com/facebook/fbthrift/thrift/lib/thrift/metadata"
)

// (needed to ensure safety because of naive import list construction)
var _ = context.Background
var _ = errors.New
var _ = fmt.Printf
var _ = io.EOF
var _ = iter.Pull[bool]
var _ = reflect.Ptr
var _ = sync.OnceFunc
var _ = thrift.VOID
var _ = metadata.GoUnusedProtection__


type BiDiService interface {
    Simple(ctx context.Context) (func(context.Context, iter.Seq2[int32, error]) error, func(context.Context, chan<- int16) error, error)
    Response(ctx context.Context) (string, func(context.Context, iter.Seq2[int32, error]) error, func(context.Context, chan<- int16) error, error)
    CanThrow(ctx context.Context) (func(context.Context, iter.Seq2[int64, error]) error, func(context.Context, chan<- int64) error, error)
}

type BiDiServiceClient interface {
    io.Closer
    Simple(ctx context.Context) (func(iter.Seq2[int32, error]), iter.Seq2[int16, error], error)
    Response(ctx context.Context) (string, func(iter.Seq2[int32, error]), iter.Seq2[int16, error], error)
    CanThrow(ctx context.Context) (func(iter.Seq2[int64, error]), iter.Seq2[int64, error], error)
}

type biDiServiceClientImpl struct {
    ch thrift.RequestChannel
}
// Compile time interface enforcer
var _ BiDiServiceClient = (*biDiServiceClientImpl)(nil)

func NewBiDiServiceChannelClient(channel thrift.RequestChannel) BiDiServiceClient {
    return &biDiServiceClientImpl{
        ch: channel,
    }
}

func init() {
    thrift.InternalRegisterClientConstructor[BiDiServiceClient](NewBiDiServiceChannelClient)
}

func (c *biDiServiceClientImpl) Close() error {
    return c.ch.Close()
}

func (c *biDiServiceClientImpl) Simple(ctx context.Context) (func(iter.Seq2[int32, error]), iter.Seq2[int16, error], error) {
    if ctx.Done() == nil {
		return nil, nil, errors.New("context does not support cancellation")
	}
    fbthriftReq := &reqBiDiServiceSimple{
    }
    fbthriftFirstResp := newRespBiDiServiceSimple()

    fbthriftChannel := c.ch

    fbthriftNewStreamElemFn := func() thrift.ReadableResult {
        return newStreamBiDiServiceSimple()
    }
    fbthriftSinkFn, fbthriftStreamSeq, fbthriftErr := fbthriftChannel.SendRequestBiDi(
        ctx,
        "simple",
        fbthriftReq,
        fbthriftFirstResp,
        fbthriftNewStreamElemFn,
    )
    if fbthriftErr != nil {
        return nil, nil, fbthriftErr
    }
    fbthriftSinkCallback := func(sinkSeq iter.Seq2[int32, error]) {
        sinkPayloadSeq := func(yield func(thrift.WritableResult, error) bool) {
            for elem, err := range sinkSeq {
                sinkPayload := newSinkBiDiServiceSimple()
                if err != nil {
                    yield(nil, err)
                    return
                }
                sinkPayload.Success = &elem
                if !yield(sinkPayload, nil) {
                    return
                }
            }
        }
        go fbthriftSinkFn(sinkPayloadSeq)
    }
    fbthriftStreamSeqAdapter := func(yield func(int16, error) bool) {
        for elem, err := range fbthriftStreamSeq {
            if err != nil {
                yield(0, err)
                return
            }
            fbthriftRes := elem.(*streamBiDiServiceSimple)
            if !yield(fbthriftRes.GetSuccess(), nil) {
                return
            }
        }
    }
    return fbthriftSinkCallback, fbthriftStreamSeqAdapter, nil
}

func (c *biDiServiceClientImpl) Response(ctx context.Context) (string, func(iter.Seq2[int32, error]), iter.Seq2[int16, error], error) {
    var fbthriftFirstRespZero string
    if ctx.Done() == nil {
		return fbthriftFirstRespZero, nil, nil, errors.New("context does not support cancellation")
	}
    fbthriftReq := &reqBiDiServiceResponse{
    }
    fbthriftFirstResp := newRespBiDiServiceResponse()

    fbthriftChannel := c.ch

    fbthriftNewStreamElemFn := func() thrift.ReadableResult {
        return newStreamBiDiServiceResponse()
    }
    fbthriftSinkFn, fbthriftStreamSeq, fbthriftErr := fbthriftChannel.SendRequestBiDi(
        ctx,
        "response",
        fbthriftReq,
        fbthriftFirstResp,
        fbthriftNewStreamElemFn,
    )
    if fbthriftErr != nil {
        return fbthriftFirstRespZero, nil, nil, fbthriftErr
    }
    fbthriftSinkCallback := func(sinkSeq iter.Seq2[int32, error]) {
        sinkPayloadSeq := func(yield func(thrift.WritableResult, error) bool) {
            for elem, err := range sinkSeq {
                sinkPayload := newSinkBiDiServiceResponse()
                if err != nil {
                    yield(nil, err)
                    return
                }
                sinkPayload.Success = &elem
                if !yield(sinkPayload, nil) {
                    return
                }
            }
        }
        go fbthriftSinkFn(sinkPayloadSeq)
    }
    fbthriftStreamSeqAdapter := func(yield func(int16, error) bool) {
        for elem, err := range fbthriftStreamSeq {
            if err != nil {
                yield(0, err)
                return
            }
            fbthriftRes := elem.(*streamBiDiServiceResponse)
            if !yield(fbthriftRes.GetSuccess(), nil) {
                return
            }
        }
    }
    return fbthriftFirstResp.GetSuccess(), fbthriftSinkCallback, fbthriftStreamSeqAdapter, nil
}

func (c *biDiServiceClientImpl) CanThrow(ctx context.Context) (func(iter.Seq2[int64, error]), iter.Seq2[int64, error], error) {
    if ctx.Done() == nil {
		return nil, nil, errors.New("context does not support cancellation")
	}
    fbthriftReq := &reqBiDiServiceCanThrow{
    }
    fbthriftFirstResp := newRespBiDiServiceCanThrow()

    fbthriftChannel := c.ch

    fbthriftNewStreamElemFn := func() thrift.ReadableResult {
        return newStreamBiDiServiceCanThrow()
    }
    fbthriftSinkFn, fbthriftStreamSeq, fbthriftErr := fbthriftChannel.SendRequestBiDi(
        ctx,
        "canThrow",
        fbthriftReq,
        fbthriftFirstResp,
        fbthriftNewStreamElemFn,
    )
    if fbthriftErr != nil {
        return nil, nil, fbthriftErr
    }
    fbthriftSinkCallback := func(sinkSeq iter.Seq2[int64, error]) {
        sinkPayloadSeq := func(yield func(thrift.WritableResult, error) bool) {
            for elem, err := range sinkSeq {
                sinkPayload := newSinkBiDiServiceCanThrow()
                if err != nil {
                    switch v := err.(type) {
                    case *BiDiSinkException:
                        sinkPayload.Ex = v
                        yield(sinkPayload, nil)
                    default:
                        yield(nil, err)
                    }
                    return
                }
                sinkPayload.Success = &elem
                if !yield(sinkPayload, nil) {
                    return
                }
            }
        }
        go fbthriftSinkFn(sinkPayloadSeq)
    }
    fbthriftStreamSeqAdapter := func(yield func(int64, error) bool) {
        for elem, err := range fbthriftStreamSeq {
            if err != nil {
                yield(0, err)
                return
            }
            fbthriftRes := elem.(*streamBiDiServiceCanThrow)
            if !yield(fbthriftRes.GetSuccess(), nil) {
                return
            }
        }
    }
    return fbthriftSinkCallback, fbthriftStreamSeqAdapter, nil
}


type BiDiServiceProcessor struct {
    processorFunctionMap map[string]thrift.ProcessorFunction
    functionServiceMap   map[string]string
    handler              BiDiService
}

func NewBiDiServiceProcessor(handler BiDiService) *BiDiServiceProcessor {
    p := &BiDiServiceProcessor{
        handler:              handler,
        processorFunctionMap: make(map[string]thrift.ProcessorFunction),
        functionServiceMap:   make(map[string]string),
    }
    p.AddToProcessorFunctionMap("simple", &procFuncBiDiServiceSimple{handler: handler})
    p.AddToProcessorFunctionMap("response", &procFuncBiDiServiceResponse{handler: handler})
    p.AddToProcessorFunctionMap("canThrow", &procFuncBiDiServiceCanThrow{handler: handler})
    p.AddToFunctionServiceMap("simple", "BiDiService")
    p.AddToFunctionServiceMap("response", "BiDiService")
    p.AddToFunctionServiceMap("canThrow", "BiDiService")

    return p
}

func (p *BiDiServiceProcessor) AddToProcessorFunctionMap(key string, processorFunction thrift.ProcessorFunction) {
    p.processorFunctionMap[key] = processorFunction
}

func (p *BiDiServiceProcessor) AddToFunctionServiceMap(key, service string) {
    p.functionServiceMap[key] = service
}

func (p *BiDiServiceProcessor) GetProcessorFunction(key string) (processor thrift.ProcessorFunction) {
    return p.processorFunctionMap[key]
}

func (p *BiDiServiceProcessor) ProcessorFunctionMap() map[string]thrift.ProcessorFunction {
    return p.processorFunctionMap
}

func (p *BiDiServiceProcessor) FunctionServiceMap() map[string]string {
    return p.functionServiceMap
}


func (p *BiDiServiceProcessor) GetInteractionProcessors() []thrift.Processor {
    return []thrift.Processor{}
}

func (p *BiDiServiceProcessor) GetThriftMetadata() *metadata.ThriftMetadata {
    return GetThriftMetadataForService("module.BiDiService")
}

type procFuncBiDiServiceSimple struct {
    handler BiDiService
}
// Compile time interface enforcer
var _ thrift.ProcessorFunction = (*procFuncBiDiServiceSimple)(nil)

func (p *procFuncBiDiServiceSimple) NewReqArgs() thrift.ReadableStruct {
    return newReqBiDiServiceSimple()
}

func (p *procFuncBiDiServiceSimple) RunContext(ctx context.Context, reqStruct thrift.ReadableStruct) (thrift.WritableStruct, error) {
    return nil, errors.New("not supported")
}

func (p *procFuncBiDiServiceSimple) NewSinkElem() thrift.ReadableResult {
    return newSinkBiDiServiceSimple()
}

func (p *procFuncBiDiServiceSimple) RunBiDiContext(
    ctx context.Context,
    reqStruct thrift.ReadableStruct,
    onFirstResponse func(thrift.WritableStruct),
    onStreamNext func(thrift.WritableStruct),
    onStreamComplete func(),
    sinkSeq iter.Seq2[thrift.ReadableStruct, error],
) {
    firstResponse := newRespBiDiServiceSimple()
    sinkConsumerFunc, streamProducerFunc, initialErr := p.handler.Simple(ctx)
    if initialErr != nil {
        internalErr := fmt.Errorf("Internal error processing Simple: %w", initialErr)
        x := thrift.NewApplicationException(thrift.INTERNAL_ERROR, internalErr.Error())
        onFirstResponse(x)
        onStreamComplete()
        return
    }

    onFirstResponse(firstResponse)

    fbthriftTypedSinkSeq := func(yield func(int32, error) bool) {
        for elem, err := range sinkSeq {
            if err != nil {
                yield(0, err)
                return
            }
            sinkWrapStruct := elem.(*sinkBiDiServiceSimple)
            if !yield(*sinkWrapStruct.Success, nil) {
                return
            }
        }
    }

    fbthriftStreamChan := make(chan int16, thrift.DefaultStreamBufferSize)
    var senderWg sync.WaitGroup
    senderWg.Add(1)
    go func() {
        defer senderWg.Done()
        for elem := range fbthriftStreamChan {
            streamWrapStruct := newStreamBiDiServiceSimple()
            streamWrapStruct.Success = &elem
            onStreamNext(streamWrapStruct)
        }
    }()

    // Run sink consumer and stream producer concurrently.
    var sinkWg sync.WaitGroup
    sinkWg.Add(1)
    go func() {
        defer sinkWg.Done()
        sinkConsumerFunc(ctx, fbthriftTypedSinkSeq)
    }()

    streamErr := streamProducerFunc(ctx, fbthriftStreamChan)
    // Stream is complete. Close the channel and wait for the sender goroutine to finish.
    close(fbthriftStreamChan)
    senderWg.Wait()
    if streamErr != nil {
        internalErr := fmt.Errorf("Internal stream handler error Simple: %w", streamErr)
        x := thrift.NewApplicationException(thrift.INTERNAL_ERROR, internalErr.Error())
        onStreamNext(x)
    }
    // Wait for sink consumer to finish before completing the stream.
    sinkWg.Wait()
    onStreamComplete()
}

type procFuncBiDiServiceResponse struct {
    handler BiDiService
}
// Compile time interface enforcer
var _ thrift.ProcessorFunction = (*procFuncBiDiServiceResponse)(nil)

func (p *procFuncBiDiServiceResponse) NewReqArgs() thrift.ReadableStruct {
    return newReqBiDiServiceResponse()
}

func (p *procFuncBiDiServiceResponse) RunContext(ctx context.Context, reqStruct thrift.ReadableStruct) (thrift.WritableStruct, error) {
    return nil, errors.New("not supported")
}

func (p *procFuncBiDiServiceResponse) NewSinkElem() thrift.ReadableResult {
    return newSinkBiDiServiceResponse()
}

func (p *procFuncBiDiServiceResponse) RunBiDiContext(
    ctx context.Context,
    reqStruct thrift.ReadableStruct,
    onFirstResponse func(thrift.WritableStruct),
    onStreamNext func(thrift.WritableStruct),
    onStreamComplete func(),
    sinkSeq iter.Seq2[thrift.ReadableStruct, error],
) {
    firstResponse := newRespBiDiServiceResponse()
    retval, sinkConsumerFunc, streamProducerFunc, initialErr := p.handler.Response(ctx)
    if initialErr != nil {
        internalErr := fmt.Errorf("Internal error processing Response: %w", initialErr)
        x := thrift.NewApplicationException(thrift.INTERNAL_ERROR, internalErr.Error())
        onFirstResponse(x)
        onStreamComplete()
        return
    }

    firstResponse.Success = &retval
    onFirstResponse(firstResponse)

    fbthriftTypedSinkSeq := func(yield func(int32, error) bool) {
        for elem, err := range sinkSeq {
            if err != nil {
                yield(0, err)
                return
            }
            sinkWrapStruct := elem.(*sinkBiDiServiceResponse)
            if !yield(*sinkWrapStruct.Success, nil) {
                return
            }
        }
    }

    fbthriftStreamChan := make(chan int16, thrift.DefaultStreamBufferSize)
    var senderWg sync.WaitGroup
    senderWg.Add(1)
    go func() {
        defer senderWg.Done()
        for elem := range fbthriftStreamChan {
            streamWrapStruct := newStreamBiDiServiceResponse()
            streamWrapStruct.Success = &elem
            onStreamNext(streamWrapStruct)
        }
    }()

    // Run sink consumer and stream producer concurrently.
    var sinkWg sync.WaitGroup
    sinkWg.Add(1)
    go func() {
        defer sinkWg.Done()
        sinkConsumerFunc(ctx, fbthriftTypedSinkSeq)
    }()

    streamErr := streamProducerFunc(ctx, fbthriftStreamChan)
    // Stream is complete. Close the channel and wait for the sender goroutine to finish.
    close(fbthriftStreamChan)
    senderWg.Wait()
    if streamErr != nil {
        internalErr := fmt.Errorf("Internal stream handler error Response: %w", streamErr)
        x := thrift.NewApplicationException(thrift.INTERNAL_ERROR, internalErr.Error())
        onStreamNext(x)
    }
    // Wait for sink consumer to finish before completing the stream.
    sinkWg.Wait()
    onStreamComplete()
}

type procFuncBiDiServiceCanThrow struct {
    handler BiDiService
}
// Compile time interface enforcer
var _ thrift.ProcessorFunction = (*procFuncBiDiServiceCanThrow)(nil)

func (p *procFuncBiDiServiceCanThrow) NewReqArgs() thrift.ReadableStruct {
    return newReqBiDiServiceCanThrow()
}

func (p *procFuncBiDiServiceCanThrow) RunContext(ctx context.Context, reqStruct thrift.ReadableStruct) (thrift.WritableStruct, error) {
    return nil, errors.New("not supported")
}

func (p *procFuncBiDiServiceCanThrow) NewSinkElem() thrift.ReadableResult {
    return newSinkBiDiServiceCanThrow()
}

func (p *procFuncBiDiServiceCanThrow) RunBiDiContext(
    ctx context.Context,
    reqStruct thrift.ReadableStruct,
    onFirstResponse func(thrift.WritableStruct),
    onStreamNext func(thrift.WritableStruct),
    onStreamComplete func(),
    sinkSeq iter.Seq2[thrift.ReadableStruct, error],
) {
    firstResponse := newRespBiDiServiceCanThrow()
    sinkConsumerFunc, streamProducerFunc, initialErr := p.handler.CanThrow(ctx)
    if initialErr != nil {
        switch v := initialErr.(type) {
        case *BiDiMethodException:
            firstResponse.Ex = v
            onFirstResponse(firstResponse)
        default:
            internalErr := fmt.Errorf("Internal error processing CanThrow: %w", initialErr)
            x := thrift.NewApplicationException(thrift.INTERNAL_ERROR, internalErr.Error())
            onFirstResponse(x)
        }
        onStreamComplete()
        return
    }

    onFirstResponse(firstResponse)

    fbthriftTypedSinkSeq := func(yield func(int64, error) bool) {
        for elem, err := range sinkSeq {
            if err != nil {
                yield(0, err)
                return
            }
            sinkWrapStruct := elem.(*sinkBiDiServiceCanThrow)
            if !yield(*sinkWrapStruct.Success, nil) {
                return
            }
        }
    }

    fbthriftStreamChan := make(chan int64, thrift.DefaultStreamBufferSize)
    var senderWg sync.WaitGroup
    senderWg.Add(1)
    go func() {
        defer senderWg.Done()
        for elem := range fbthriftStreamChan {
            streamWrapStruct := newStreamBiDiServiceCanThrow()
            streamWrapStruct.Success = &elem
            onStreamNext(streamWrapStruct)
        }
    }()

    // Run sink consumer and stream producer concurrently.
    var sinkWg sync.WaitGroup
    sinkWg.Add(1)
    go func() {
        defer sinkWg.Done()
        sinkConsumerFunc(ctx, fbthriftTypedSinkSeq)
    }()

    streamErr := streamProducerFunc(ctx, fbthriftStreamChan)
    // Stream is complete. Close the channel and wait for the sender goroutine to finish.
    close(fbthriftStreamChan)
    senderWg.Wait()
    if streamErr != nil {
        streamWrapStruct := newStreamBiDiServiceCanThrow()
        switch v := streamErr.(type) {
        case *BiDiStreamException:
            streamWrapStruct.Ex = v
            onStreamNext(streamWrapStruct)
        default:
            internalErr := fmt.Errorf("Internal stream handler error CanThrow: %w", streamErr)
            x := thrift.NewApplicationException(thrift.INTERNAL_ERROR, internalErr.Error())
            onStreamNext(x)
        }
    }
    // Wait for sink consumer to finish before completing the stream.
    sinkWg.Wait()
    onStreamComplete()
}


