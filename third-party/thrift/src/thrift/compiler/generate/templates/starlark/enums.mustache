{{#program:enums}}
__{{enum:name}}_enum__ = record(
  {{#enum:values}}
    {{enum_value:name}} = int.type,
  {{/enum:values}}
)

{{enum:name}} = __{{enum:name}}_enum__(
  {{#enum:values}}
    {{enum_value:name}} = {{enum_value:value}},
  {{/enum:values}}
)

{{/program:enums}}
