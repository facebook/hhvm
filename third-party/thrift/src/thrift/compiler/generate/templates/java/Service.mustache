{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}
{{#import "common/common" as common}}
{{#import "common/types" as types}}
{{#partial common.auto_generated}}

package {{service.program.javaPackage}};

import com.facebook.swift.codec.*;
import com.facebook.swift.codec.ThriftField.Requiredness;
import com.facebook.swift.service.*;
import com.facebook.thrift.client.*;
import com.facebook.thrift.client.ResponseWrapper;
import com.facebook.thrift.client.RpcOptions;
import com.google.common.util.concurrent.ListenableFuture;
import java.io.*;
import java.util.*;
import reactor.core.publisher.Mono;

@SwiftGenerated
@com.facebook.swift.service.ThriftService("{{service.javaCapitalName}}")
public interface {{service.javaCapitalName}} extends java.io.Closeable, com.facebook.thrift.util.BlockingService{{#if (object.notnull? service.extends)}}, {{service.extends.java_qualified_name}}{{/if}} {
    static com.facebook.thrift.server.RpcServerHandlerBuilder<{{service.javaCapitalName}}> serverHandlerBuilder({{service.javaCapitalName}} _serverImpl) {
        return new com.facebook.thrift.server.RpcServerHandlerBuilder<{{service.javaCapitalName}}>(_serverImpl) {
                @java.lang.Override
                public com.facebook.thrift.server.RpcServerHandler build() {
                return new {{service.javaCapitalName}}RpcServerHandler(impl, eventHandlers);
            }
        };
    }

    static com.facebook.thrift.server.ThriftMetadataHandler getMetadataHandler() {
        return new {{service.javaCapitalName}}ThriftMetadataHandler();
    }

    static com.facebook.thrift.client.ClientBuilder<{{service.javaCapitalName}}> clientBuilder() {
        return new ClientBuilder<{{service.javaCapitalName}}>() {
            @java.lang.Override
            public {{service.javaCapitalName}} build(Mono<RpcClient> rpcClientMono) {
                {{service.javaCapitalName}}.Reactive _delegate =
                    new {{service.javaCapitalName}}ReactiveClient(protocolId, rpcClientMono, headersMono, persistentHeadersMono);
                return new {{service.javaCapitalName}}ReactiveBlockingWrapper(_delegate);
            }
        };
    }

    @com.facebook.swift.service.ThriftService("{{service.javaCapitalName}}")
    public interface Async extends java.io.Closeable, com.facebook.thrift.util.AsyncService{{#if (object.notnull? service.extends)}}, {{service.extends.java_qualified_name}}.Async{{/if}} {
        static com.facebook.thrift.server.RpcServerHandlerBuilder<{{service.javaCapitalName}}.Async> serverHandlerBuilder({{service.javaCapitalName}}.Async _serverImpl) {
            return new com.facebook.thrift.server.RpcServerHandlerBuilder<{{service.javaCapitalName}}.Async>(_serverImpl) {
                @java.lang.Override
                public com.facebook.thrift.server.RpcServerHandler build() {
                    return new {{service.javaCapitalName}}RpcServerHandler(impl, eventHandlers);
                }
            };
        }

        static com.facebook.thrift.client.ClientBuilder<{{service.javaCapitalName}}.Async> clientBuilder() {
            return new ClientBuilder<{{service.javaCapitalName}}.Async>() {
                @java.lang.Override
                public {{service.javaCapitalName}}.Async build(Mono<RpcClient> rpcClientMono) {
                    {{service.javaCapitalName}}.Reactive _delegate =
                        new {{service.javaCapitalName}}ReactiveClient(protocolId, rpcClientMono, headersMono, persistentHeadersMono);
                    return new {{service.javaCapitalName}}ReactiveAsyncWrapper(_delegate);
                }
            };
        }

        @java.lang.Override void close();

        {{#each (array.enumerate service.single_request_functions with_last=true) as |i func last?|}}
        @ThriftMethod(value = "{{func.name}}"{{#if func.oneway?}},
                      oneway = true{{/if func.oneway?}}{{#each (array.enumerate func.exceptions with_first=true with_last=true) as |i field first? last?|}}{{#if first?}},
                      exception = { {{/if first?}}
                          @ThriftException(type={{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}}.class, id={{field.id}}){{#if (not last?)}},{{/if (not last?)}}{{#if last?}}
                      }{{/if last?}}{{/each}})
        ListenableFuture<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}
            @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}},{{/if (not last?)}}{{/each}});

        default ListenableFuture<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each func.params.fields as |field|}}
            @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
            RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }

        default ListenableFuture<ResponseWrapper<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}>> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}
            @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
            RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }
        {{#if (not last?)}}

        {{/if (not last?)}}
        {{/each}}
        {{#each service.interactions as |interaction|}}
        @com.facebook.swift.service.ThriftInteraction("{{interaction.name}}")
        public interface {{interaction.name}} extends java.io.Closeable {
            {{#each interaction.single_request_functions as |func|}}
            @ThriftMethod(value = "{{func.name}}"{{#if func.oneway?}},
                oneway = true{{/if func.oneway?}}{{#each (array.enumerate func.exceptions with_first=true with_last=true) as |i field first? last?|}}{{#if first?}},
                exception = { {{/if first?}}
                    @ThriftException(type={{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}}.class, id={{field.id}}){{#if (not last?)}},{{/if (not last?)}}{{#if last?}}
                }{{/if last?}}{{/each}})
            ListenableFuture<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}} {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}},{{/if (not last?)}}{{/each}});

            ListenableFuture<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each func.params.fields as |field|}}
                @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
                RpcOptions rpcOptions);

            ListenableFuture<ResponseWrapper<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}>> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}
                @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
                RpcOptions rpcOptions);
            {{/each}}

            @java.lang.Override
            void close();
        }

        public {{interaction.name}} create{{interaction.name}}();

        {{/each}}
    }
    @java.lang.Override void close();
    {{#each service.single_request_functions as |func|}}

    @ThriftMethod(value = "{{func.name}}"{{#if func.oneway?}},
                  oneway = true{{/if func.oneway?}}{{#each (array.enumerate func.exceptions with_first=true with_last=true) as |i field first? last?|}}{{#if first?}},
                  exception = { {{/if first?}}
                      @ThriftException(type={{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}}.class, id={{field.id}}){{#if (not last?)}},{{/if (not last?)}}{{#if last?}}
                  }{{/if last?}}{{/each}})
    {{#partial types.java_type type=func.return_type has_adapter_override=false}} {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}
        @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}},{{/if (not last?)}}{{/each}}){{!
          Exceptions
        }} throws {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}, {{/each}}org.apache.thrift.TException;

    default {{#partial types.java_type type=func.return_type has_adapter_override=false}} {{func.java_name}}({{#each func.params.fields as |field|}}
        @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
        RpcOptions rpcOptions){{!
          Exceptions
        }} throws {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}, {{/each}}org.apache.thrift.TException {
        throw new UnsupportedOperationException();
    }

    default ResponseWrapper<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}
        @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
        RpcOptions rpcOptions){{!
          Exceptions
        }} throws {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}, {{/each}}org.apache.thrift.TException {
        throw new UnsupportedOperationException();
    }
    {{/each}}

    @com.facebook.swift.service.ThriftService("{{service.javaCapitalName}}")
    interface Reactive extends reactor.core.Disposable, com.facebook.thrift.util.ReactiveService{{#if (object.notnull? service.extends)}}, {{service.extends.java_qualified_name}}.Reactive{{/if}} {
        static com.facebook.thrift.server.RpcServerHandlerBuilder<{{service.javaCapitalName}}.Reactive> serverHandlerBuilder({{service.javaCapitalName}}.Reactive _serverImpl) {
            return new com.facebook.thrift.server.RpcServerHandlerBuilder<{{service.javaCapitalName}}.Reactive>(_serverImpl) {
                @java.lang.Override
                public com.facebook.thrift.server.RpcServerHandler build() {
                    return new {{service.javaCapitalName}}RpcServerHandler(impl, eventHandlers);
                }
            };
        }

        static com.facebook.thrift.client.ClientBuilder<{{service.javaCapitalName}}.Reactive> clientBuilder() {
            return new ClientBuilder<{{service.javaCapitalName}}.Reactive>() {
                @java.lang.Override
                public {{service.javaCapitalName}}.Reactive build(Mono<RpcClient> rpcClientMono) {
                    return new {{service.javaCapitalName}}ReactiveClient(protocolId, rpcClientMono, headersMono, persistentHeadersMono);
                }
            };
        }

        {{#each service.single_request_functions as |func|}}
        @ThriftMethod(value = "{{func.name}}"{{#if func.oneway?}},
                  oneway = true{{/if func.oneway?}}{{#each (array.enumerate func.exceptions with_first=true with_last=true) as |i field first? last?|}}{{#if first?}},
                  exception = { {{/if first?}}
                      @ThriftException(type={{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}}.class, id={{field.id}}){{#if (not last?)}},{{/if (not last?)}}{{#if last?}}
                  }{{/if last?}}{{/each}})
        reactor.core.publisher.Mono<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}});

        default reactor.core.publisher.Mono<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }

        default reactor.core.publisher.Mono<ResponseWrapper<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}>> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }

        {{/each}}
        {{#each service.streaming_functions as |func|}}
        reactor.core.publisher.Flux<{{#partial types.stream_return_type function=func}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}});

        default reactor.core.publisher.Flux<{{#partial types.stream_return_type function=func}}> {{func.java_name}}({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }

        default reactor.core.publisher.Flux<ResponseWrapper<{{#partial types.stream_return_type function=func}}>> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }

        {{/each}}
        {{#each service.sink_functions as |func|}}
        {{#partial types.sink_return_type function=func}} {{func.java_name}}({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}} org.reactivestreams.Publisher<{{#partial types.boxed_type type=func.sink.elem_type field_hasAdapter=false}}> payloads);

        default {{#partial types.sink_return_type function=func}} {{func.java_name}}({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}} org.reactivestreams.Publisher<{{#partial types.boxed_type type=func.sink.elem_type field_hasAdapter=false}}> payloads, RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }

        default {{#partial types.sink_wrapper_return_type function=func}} {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}} org.reactivestreams.Publisher<{{#partial types.boxed_type type=func.sink.elem_type field_hasAdapter=false}}> payloads, RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }

        {{/each}}
        {{#each service.interactions as |interaction|}}
        @com.facebook.swift.service.ThriftInteraction("{{interaction.name}}")
        public interface {{interaction.name}} extends reactor.core.Disposable {
            {{#each interaction.single_request_functions as |func|}}
            @ThriftMethod(value = "{{func.name}}"{{#if func.oneway?}},
                oneway = true{{/if func.oneway?}}{{#each (array.enumerate func.exceptions with_first=true with_last=true) as |i field first? last?|}}{{#if first?}},
                exception = { {{/if first?}}
                    @ThriftException(type={{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}}.class, id={{field.id}}){{#if (not last?)}},{{/if (not last?)}}{{#if last?}}
                }{{/if last?}}{{/each}})
            reactor.core.publisher.Mono<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}} final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}},{{/if (not last?)}}{{/each}});

            reactor.core.publisher.Mono<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions);

            reactor.core.publisher.Mono<ResponseWrapper<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}>> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions);

            {{/each}}
            {{#each interaction.streaming_functions as |func|}}
            reactor.core.publisher.Flux<{{#partial types.stream_return_type function=func}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}});

            default reactor.core.publisher.Flux<{{#partial types.stream_return_type function=func}}> {{func.java_name}}({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions) {
                throw new UnsupportedOperationException();
            }

            default reactor.core.publisher.Flux<ResponseWrapper<{{#partial types.stream_return_type function=func}}>> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions) {
                throw new UnsupportedOperationException();
            }

            {{/each}}
            @java.lang.Override
            void dispose();
        }

        public {{interaction.name}} create{{interaction.name}}();

        {{/each}}
    }
    {{#each service.interactions as |interaction|}}
    @com.facebook.swift.service.ThriftInteraction("{{interaction.name}}")
    public interface {{interaction.name}} extends java.io.Closeable {
        {{#each interaction.single_request_functions as |func|}}
        @ThriftMethod(value = "{{func.name}}"{{#if func.oneway?}},
                      oneway = true{{/if func.oneway?}}{{#each (array.enumerate func.exceptions with_first=true with_last=true) as |i field first? last?|}}{{#if first?}},
                      exception = { {{/if first?}}
                          @ThriftException(type={{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}}.class, id={{field.id}}){{#if (not last?)}},{{/if (not last?)}}{{#if last?}}
                      }{{/if last?}}{{/each}})
        {{#partial types.java_type type=func.return_type has_adapter_override=false}} {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}
        @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}},{{/if (not last?)}}{{/each}}) throws {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}, {{/each}}org.apache.thrift.TException;

        {{#partial types.java_type type=func.return_type has_adapter_override=false}} {{func.java_name}}({{#each func.params.fields as |field|}}
            @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
            RpcOptions rpcOptions){{!
            Exceptions
            }} throws {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}, {{/each}}org.apache.thrift.TException;

        ResponseWrapper<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}
            @com.facebook.swift.codec.ThriftField(value={{field.id}}, {{#if field.negativeId?}}isLegacyId=true, {{/if field.negativeId?}}name="{{field.name}}", requiredness={{#partial types.requiredness field=field}}) final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
            RpcOptions rpcOptions){{!
            Exceptions
            }} throws {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}, {{/each}}org.apache.thrift.TException;
        {{/each}}

        @java.lang.Override void close();
    }

    {{interaction.name}} create{{interaction.name}}();

    {{/each}}
}
