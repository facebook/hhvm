{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}
{{! Serialize a function parameter, applying type-level adapter conversion
    if needed. See type_adapter_read in lib/errors.mustache for why the `type:`
    prototype qualifier is used here. }}
  {{#let partial type_adapter_write_param |param| captures |types|}}
{{#pragma ignore-newlines}}
  {{#if param.type.type:has_adapter?}}
::fbthrift::Serialize::rs_thrift_write(&{{param.type.type:adapter_qualified}}::to_thrift_field::<fbthrift::metadata::NoThriftAnnotations>(&self.{{param.rust_name}}, {{param.id}}), p)
  {{#else}}
{{#partial types.write type=param.type}}(&self.{{param.rust_name}}, p)
  {{/if param.type.type:has_adapter?}}
  {{/let partial}}
  {{#let service_extends = (if interface.interaction? null interface.extends)}}
{{> lib/client_trait}}

/// Client definitions for `{{interface.rust_name}}`.
pub struct {{interface.rust_name}}Impl<P, T, S = ::fbthrift::NoopSpawner> {
  {{#if interface.interaction?}}
    #[allow(dead_code)]
    names: &'static {{interface.rust_name}}Names,
  {{/if interface.interaction?}}
  {{#if (object.notnull? service_extends)}}
    parent: {{service_extends.client_package}}::{{service_extends.rust_name}}Impl<P, T, S>,
  {{#else}}
    transport: T,
    _phantom: ::std::marker::PhantomData<fn() -> (P, S)>,
  {{/if (object.notnull? service_extends)}}
}

  {{#if interface.interaction?}}
pub struct {{interface.rust_name}}Names {
    pub service: &'static ::std::ffi::CStr,
    {{#each interface.functions as |function|}}
    pub method_{{function.name}}: &'static ::std::ffi::CStr,
    {{/each}}
}
  {{/if interface.interaction?}}

impl<P, T, S> {{interface.rust_name}}Impl<P, T, S>
where
    P: ::fbthrift::Protocol,
    T: ::fbthrift::Transport,
    {{! require P::Frame and T to have compatible DecBuf and EncBuf::Final }}
    P::Frame: ::fbthrift::Framing<DecBuf = ::fbthrift::FramingDecoded<T>>,
    ::fbthrift::ProtocolEncoded<P>: ::fbthrift::BufMutExt<Final = ::fbthrift::FramingEncodedFinal<T>>,
    P::Deserializer: ::std::marker::Send,
    S: ::fbthrift::help::Spawner,
{
    pub fn new(
        transport: T,
      {{#if interface.interaction?}}
        names: &'static {{interface.rust_name}}Names,
      {{/if interface.interaction?}}
    ) -> Self {
        Self {
          {{#if interface.interaction?}}
            names,
            transport,
            _phantom: ::std::marker::PhantomData,
          {{#else if (object.notnull? service_extends)}}
            parent: {{service_extends.client_package}}::{{service_extends.rust_name}}Impl::<P, T, S>::new(transport),
          {{#else}}
            transport,
            _phantom: ::std::marker::PhantomData,
          {{/if interface.interaction?}}
        }
    }

    pub fn transport(&self) -> &T {
        ::fbthrift::help::GetTransport::transport(self)
    }

  {{#if (not interface.interaction?)}}
    {{#each interface.interactions as |interaction|}}
    const NAMES_{{interaction.rust_name}}: {{interaction.client_package}}::{{interaction.rust_name}}Names = {{interaction.client_package}}::{{interaction.rust_name}}Names {
        service: c"{{interface.name}}",
        {{#each interaction.functions as |function|}}
        method_{{function.name}}: c"{{interface.name}}.{{interaction.name}}.{{function.name}}",
        {{/each}}
    };
    {{/each}}
  {{/if (not interface.interaction?)}}

    {{#each interface.functions as |function|}}
    {{#if (not function.starts_interaction?)}}

    fn _{{function.name}}_impl(
        &self,
      {{#each function.params.fields as |param|}}
        arg_{{param.name}}: {{#partial rpc.arg field=param}},
      {{/each}}
        rpc_options: T::RpcOptions,
    ) -> {{#partial rpc.client_return_type function=function service=interface}} {
        use ::tracing::Instrument as _;
        use ::futures::FutureExt as _;
        {{#if function.bidirectional_stream?}}
        use ::futures::StreamExt as _;
        use ::fbthrift::Deserialize as _;
        use ::futures::TryFutureExt as _;
        {{#else if function.stream?}}
        use ::futures::StreamExt as _;
        use ::fbthrift::Deserialize as _;
        {{#else if function.sink?}}
        use ::futures::StreamExt as _;
        use ::fbthrift::Deserialize as _;
        use ::futures::TryFutureExt as _;
        {{/if function.bidirectional_stream?}}

        let service_name = {{#if interface.interaction?}}self.names.service{{#else}}c"{{interface.name}}"{{/if}};
        let service_method_name = {{#if interface.interaction?}}self.names.method_{{function.name}}{{#else}}c"{{interface.name}}.{{function.name}}"{{/if}};

        {{#if function.creates_interaction?}}
        const INTERACTION_NAME: &::std::ffi::CStr = c"{{function.interaction_name}}";
        {{/if function.creates_interaction?}}
        let args = self::Args_{{interface.rust_name}}_{{function.name}} {
        {{#each function.params.fields as |param|}}
            {{param.rust_name}}: arg_{{param.name}},
        {{/each}}
            _phantom: ::std::marker::PhantomData,
        };

        {{#if function.creates_interaction?}}
        let interaction_transport = match self.transport().create_interaction(INTERACTION_NAME) {
            ::std::result::Result::Ok(res) => res,
            ::std::result::Result::Err(err) => return ::futures::future::err(err.into()).boxed(),
        };
        let interaction_impl = {{program.crate}}::{{function.interaction_name}}Impl::<P, T, S>::new(interaction_transport, &Self::NAMES_{{function.interaction_name}});
        let transport = interaction_impl.transport();
        {{#else}}
        let transport = self.transport();
        {{/if function.creates_interaction?}}

        // need to do call setup outside of async block because T: Transport isn't Send
        let request_env = match ::fbthrift::help::serialize_request_envelope::<P, _>("{{#if interface.interaction?}}{{interface.name}}.{{/if}}{{function.name}}", &args) {
            ::std::result::Result::Ok(res) => res,
            ::std::result::Result::Err(err) => return ::futures::future::err(err.into()).boxed(),
        };

        {{#if function.bidirectional_stream?}}
        let call_bidirectional = transport
            .call_bidirectional(service_name, service_method_name, request_env, rpc_options)
            .instrument(::tracing::trace_span!("call_bidirectional", method = "{{interface.name}}.{{function.name}}"));
        {{#else if function.stream?}}
        let call_stream = transport
            .call_stream(service_name, service_method_name, request_env, rpc_options)
            .instrument(::tracing::trace_span!("call_stream", method = "{{interface.name}}.{{function.name}}"));
        {{#else if function.sink?}}
        let call_sink = transport
            .call_sink(service_name, service_method_name, request_env, rpc_options)
            .instrument(::tracing::trace_span!("call_sink", method = "{{interface.name}}.{{function.name}}"));
        {{#else}}
        let call = transport
            .call(service_name, service_method_name, request_env, rpc_options)
            .instrument(::tracing::trace_span!("call", method = "{{interface.name}}.{{function.name}}"));
        {{/if function.bidirectional_stream?}}

        async move {
        {{#if function.bidirectional_stream?}}{{!

            ******************************
            *** BIDIRECTIONAL BODY ******
            ******************************

        }}
            let ::fbthrift::BidiReply {
                initial_response: _initial_response,
                stream,
                sink_processor,
            } = call_bidirectional.await?;

            // Deserialize the initial response (should be empty for bidirectional)
            let de = P::deserializer(_initial_response);
            let _initial_result = ::fbthrift::help::async_deserialize_response_envelope::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}Reader, S>(de).await??;

            // Process the server-to-client stream
            let server_stream = stream.then(|item_res| {
                async move {
                    match item_res {
                        ::std::result::Result::Err(err) =>
                            ::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}StreamError::from(err)),
                        ::std::result::Result::Ok(item_enc) => {
                            S::spawn(move || {
                                match item_enc {
                                    ::fbthrift::ClientStreamElement::Reply(payload) => {
                                        let mut de = P::deserializer(payload);
                                        <{{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}StreamReader as ::fbthrift::help::DeserializeExn>::read_result(&mut de)
                                    }
                                    ::fbthrift::ClientStreamElement::ApplicationEx(payload) => {
                                        let mut de = P::deserializer(payload);
                                        let aexn = ::fbthrift::ApplicationException::rs_thrift_read(&mut de)?;
                                        ::std::result::Result::Ok(::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}StreamError::ApplicationException(aexn)))
                                    }
                                    ::fbthrift::ClientStreamElement::DeclaredEx(payload) => {
                                        let mut de = P::deserializer(payload);
                                        <{{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}StreamReader as ::fbthrift::help::DeserializeExn>::read_result(&mut de)
                                    }
                                }
                            }).await.map_err(::anyhow::Error::from)??
                        }
                    }
                }
            })
            .boxed();

            // Create the sink handler that accepts client stream and sends to server
            let sink_helper_fn = move |stream: ::futures::stream::BoxStream<'static, ::std::result::Result<
                {{#partial types.type_name type=function.sink.elem_type}},
                {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError
            >>| {
                let new_stream = stream.map(|item| {
                    match item {
                        ::std::result::Result::Ok(res) => {
                            let enc = ::fbthrift::help::serialize_stream_item::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>(::std::result::Result::Ok(res), "{{#if interface.interaction?}}{{interface.name}}.{{/if}}{{function.name}}");
                            ::fbthrift::ClientStreamElement::Reply(enc)
                        }
                        ::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError::ApplicationException(aexn)) => {
                            let enc = ::fbthrift::help::serialize_stream_item::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>(::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError::ApplicationException(aexn)), "{{#if interface.interaction?}}{{interface.name}}.{{/if}}{{function.name}}");
                            ::fbthrift::ClientStreamElement::ApplicationEx(enc)
                        }
                        ::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError::ThriftError(err)) => {
                            let enc = ::fbthrift::help::serialize_stream_item::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>(::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError::ThriftError(err)), "{{#if interface.interaction?}}{{interface.name}}.{{/if}}{{function.name}}");
                            ::fbthrift::ClientStreamElement::ApplicationEx(enc)
                        }
          {{#if (not (array.empty? function.sink.exceptions))}}
                        ::std::result::Result::Err(other_err) => {
                            let enc = ::fbthrift::help::serialize_stream_item::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>(::std::result::Result::Err(other_err), "{{#if interface.interaction?}}{{interface.name}}.{{/if}}{{function.name}}");
                            ::fbthrift::ClientStreamElement::DeclaredEx(enc)
                        }
          {{/if (not (array.empty? function.sink.exceptions))}}
                    }
                }).boxed();
                let sinker = sink_processor;
                async move {
                    let _payload = (sinker)(new_stream).await?;
                    // For bidirectional streaming, we don't expect a final response
                    ::std::result::Result::Ok(())
                }.boxed()
            };

            let res = ::std::result::Result::Ok((server_stream, Box::new(sink_helper_fn) as Box<dyn ::std::ops::FnOnce(::futures::stream::BoxStream<'static, ::std::result::Result<{{#partial types.type_name type=function.sink.elem_type}}, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>>) -> ::futures::future::BoxFuture<'static, ::std::result::Result<(), {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>> + ::std::marker::Send>));
        {{#else if function.stream?}}{{!

            ******************************
            ******* STREAMING BODY *******
            ******************************

        }}
            let (initial, stream) = call_stream.await?;

            let new_stream = stream.then(|item_res| {
                async move {
                    match item_res {
                        ::std::result::Result::Err(err) =>
                            ::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}StreamError::from(err)),
                        ::std::result::Result::Ok(item_enc) => {
                            S::spawn(move || {
                                match item_enc {
                                    ::fbthrift::ClientStreamElement::Reply(payload) => {
                                        let mut de = P::deserializer(payload);
                                        <{{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}StreamReader as ::fbthrift::help::DeserializeExn>::read_result(&mut de)
                                    }
                                    ::fbthrift::ClientStreamElement::ApplicationEx(payload) => {
                                        let mut de = P::deserializer(payload);
                                        let aexn = ::fbthrift::ApplicationException::rs_thrift_read(&mut de)?;
                                        ::std::result::Result::Ok(::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}StreamError::ApplicationException(aexn)))
                                    }
                                    ::fbthrift::ClientStreamElement::DeclaredEx(payload) => {
                                        let mut de = P::deserializer(payload);
                                        <{{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}StreamReader as ::fbthrift::help::DeserializeExn>::read_result(&mut de)
                                    }
                                }
                            }).await.map_err(::anyhow::Error::from)??
                        }
                    }
                }
            })
            .boxed();

            let de = P::deserializer(initial);
            let res = ::fbthrift::help::async_deserialize_response_envelope::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}Reader, S>(de)
                .await??
                .map(move {{#if function.stream_has_first_response?}}|initial| (initial, new_stream){{#else}}|_| new_stream{{/if}});
        {{#else if function.sink?}}{{!

            ******************************
            **** SINK BODY ****
            ******************************

        }}
            let ::fbthrift::SinkReply {
                initial_response: _initial_response,
                sink_processor,
            } = call_sink.await?;

          {{#if function.sink_has_first_response?}}
            let de = P::deserializer(_initial_response);
            let res = ::fbthrift::help::async_deserialize_response_envelope::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}Reader, S>(de).await?;

            let initial_response = match res {
                ::std::result::Result::Ok(res) => res,
                ::std::result::Result::Err(aexn) => {
                    ::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}Error::ApplicationException(aexn))
                }
            }?;
          {{#else}}
            let initial_response = ();
          {{/if function.sink_has_first_response?}}

            let sink_helper_fn = move |stream: ::futures::stream::BoxStream<'static, ::std::result::Result<
                {{#partial types.type_name type=function.sink.elem_type}},
                {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError
            >>| {
                {{!TODO (seanlawlor): This decoding should be done on the blocking thread, not the IO thread. }}
                let new_stream = stream.map(|item| {
                    match item {
                        ::std::result::Result::Ok(res) => {
                            let enc = ::fbthrift::help::serialize_stream_item::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>(::std::result::Result::Ok(res), "{{#if interface.interaction?}}{{interface.name}}.{{/if}}{{function.name}}");
                            ::fbthrift::ClientStreamElement::Reply(enc)
                        }
                        ::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError::ApplicationException(aexn)) => {
                            let enc = ::fbthrift::help::serialize_stream_item::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>(::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError::ApplicationException(aexn)), "{{#if interface.interaction?}}{{interface.name}}.{{/if}}{{function.name}}");
                            ::fbthrift::ClientStreamElement::ApplicationEx(enc)
                        }
                        ::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError::ThriftError(err)) => {
                            let enc = ::fbthrift::help::serialize_stream_item::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>(::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError::ThriftError(err)), "{{#if interface.interaction?}}{{interface.name}}.{{/if}}{{function.name}}");
                            ::fbthrift::ClientStreamElement::ApplicationEx(enc)
                        }
                        {{#if (not (array.empty? function.sink.exceptions))}}
                        ::std::result::Result::Err(other_err) => {
                            let enc = ::fbthrift::help::serialize_stream_item::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError>(::std::result::Result::Err(other_err), "{{#if interface.interaction?}}{{interface.name}}.{{/if}}{{function.name}}");
                            ::fbthrift::ClientStreamElement::DeclaredEx(enc)
                        }
                        {{/if (not (array.empty? function.sink.exceptions))}}
                    }
                }).boxed();
                let sinker = sink_processor;
                async move {
                    let payload = (sinker)(new_stream).await?;
                    S::spawn(move || {
                        let mut de = P::deserializer(payload);
                        <{{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkFinalReader as ::fbthrift::help::DeserializeExn>::read_result(&mut de)
                    }).await.map_err(::anyhow::Error::from)??
                }.boxed()
            };

            let res = Ok(::fbthrift::SinkResult::<
                {{#if function.sink_has_first_response?}}{{#partial types.type_name type=function.return_type}}{{#else}}(){{/if}},
                {{#partial types.type_name type=function.sink.elem_type}},
                {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkError,
                {{#partial types.type_name type=function.sink.final_response_type}},
                {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}SinkFinalError,
            > {
                initial_response,
                sink: Box::new(sink_helper_fn),
            });
        {{#else}}{{!

            ******************************
            **** SINGLE RESPONSE BODY ****
            ******************************

        }}
            let reply_env = call.await?;

            let de = P::deserializer(reply_env);
            let res = ::fbthrift::help::async_deserialize_response_envelope::<P, {{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}Reader, S>(de).await?;

            let res = match res {
                ::std::result::Result::Ok(res) => res,
                ::std::result::Result::Err(aexn) => {
                    ::std::result::Result::Err({{program.crate}}::errors::{{interface.snake}}::{{function.upcamel}}Error::ApplicationException(aexn))
                }
            };
        {{/if function.bidirectional_stream?}}
        {{#if function.creates_interaction?}}{{!

            ***********************************
            **** Add interaction to result ****
            ***********************************

        }}
            let interaction_client: {{program.crate}}::client::{{function.interaction_name}}Client = ::std::sync::Arc::new(interaction_impl);
          {{#if function.stream?}}
            ::std::result::Result::Ok((interaction_client, res?))
          {{#else if function.return_type.void?}}
            res?;
            ::std::result::Result::Ok(interaction_client)
          {{#else}}
            ::std::result::Result::Ok((interaction_client, res?))
          {{/if function.stream?}}
        {{#else}}
            res
        {{/if function.creates_interaction?}}
        }
        .instrument(::tracing::info_span!("stream", method = "{{interface.name}}.{{function.name}}"))
        .boxed()
    }
    {{/if (not function.starts_interaction?)}}
  {{/each}}
}

impl<P, T, S> ::fbthrift::help::GetTransport<T> for {{interface.rust_name}}Impl<P, T, S>
where
    T: ::fbthrift::Transport,
{
    fn transport(&self) -> &T {
        {{#if (object.notnull? service_extends)}}self.parent.transport(){{#else}}&self.transport{{/if}}
    }
}

  {{#if (not interface.interaction?)}}
    {{#each (array.enumerate interface.all_parent_services with_first=true) as |_index ancestor first?|}}
#[allow(deprecated)]{{! `dependencies` modules are deprecated other than for packagePrefix to use }}
impl<'a, P, T, S> ::std::convert::AsRef<dyn {{ancestor.client_package}}::{{ancestor.rust_name}} + 'a> for {{interface.rust_name}}Impl<P, T, S>
where
    P: ::fbthrift::Protocol,
    T: ::fbthrift::Transport,
    {{! require P::Frame and T to have compatible DecBuf and EncBuf::Final }}
    P::Frame: ::fbthrift::Framing<DecBuf = ::fbthrift::FramingDecoded<T>>,
    ::fbthrift::ProtocolEncoded<P>: ::fbthrift::BufMutExt<Final = ::fbthrift::FramingEncodedFinal<T>>,
    P::Deserializer: ::std::marker::Send,
    S: ::fbthrift::help::Spawner,
{
    fn as_ref(&self) -> &(dyn {{ancestor.client_package}}::{{ancestor.rust_name}} + 'a)
    {
        {{#if first?}}&self.parent{{#else}}self.parent.as_ref(){{/if}}
    }
}

#[allow(deprecated)]{{! `dependencies` modules are deprecated other than for packagePrefix to use }}
impl<'a, P, T, S> ::std::convert::AsRef<dyn {{ancestor.client_package}}::{{ancestor.rust_name}}Ext<T> + 'a> for {{interface.rust_name}}Impl<P, T, S>
where
    P: ::fbthrift::Protocol,
    T: ::fbthrift::Transport,
    {{! require P::Frame and T to have compatible DecBuf and EncBuf::Final }}
    P::Frame: ::fbthrift::Framing<DecBuf = ::fbthrift::FramingDecoded<T>>,
    ::fbthrift::ProtocolEncoded<P>: ::fbthrift::BufMutExt<Final = ::fbthrift::FramingEncodedFinal<T>>,
    P::Deserializer: ::std::marker::Send,
    S: ::fbthrift::help::Spawner,
{
    fn as_ref(&self) -> &(dyn {{ancestor.client_package}}::{{ancestor.rust_name}}Ext<T> + 'a)
    {
        {{#if first?}}&self.parent{{#else}}self.parent.as_ref(){{/if}}
    }
}

    {{/each}}
  {{/if (not interface.interaction?)}}

  {{#each interface.functions as |function|}}
    {{#if (not function.starts_interaction?)}}

struct Args_{{interface.rust_name}}_{{function.name}}<'a> {
      {{#each function.params.fields as |param|}}
    {{param.rust_name}}: {{#partial rpc.arg_life field=param}},
      {{/each}}
    _phantom: ::std::marker::PhantomData<&'a ()>,
}

impl<'a, P: ::fbthrift::ProtocolWriter> ::fbthrift::Serialize<P> for self::Args_{{interface.rust_name}}_{{function.name}}<'a> {
    #[inline]{{! No cost because there's only one caller; with luck will mitigate move cost of args. }}
    #[::tracing::instrument(skip_all, level = "trace", name = "serialize_args", fields(method = "{{interface.name}}.{{function.name}}"))]
    fn rs_thrift_write(&self, p: &mut P) {
        p.write_struct_begin("args");
      {{#each function.params.fields as |param|}}
        p.write_field_begin("{{param.name}}", {{#partial types.protocol_ttype type=param.type}}, {{param.id}}i16);
        {{#partial type_adapter_write_param param=param}};
        p.write_field_end();
      {{/each}}
        p.write_field_stop();
        p.write_struct_end();
    }
}
    {{/if (not function.starts_interaction?)}}
  {{/each}}

impl<P, T, S> {{interface.rust_name}} for {{interface.rust_name}}Impl<P, T, S>
where
    P: ::fbthrift::Protocol,
    T: ::fbthrift::Transport,
    {{! require P::Frame and T to have compatible DecBuf and EncBuf::Final }}
    P::Frame: ::fbthrift::Framing<DecBuf = ::fbthrift::FramingDecoded<T>>,
    ::fbthrift::ProtocolEncoded<P>: ::fbthrift::BufMutExt<Final = ::fbthrift::FramingEncodedFinal<T>>,
    P::Deserializer: ::std::marker::Send,
    S: ::fbthrift::help::Spawner,
{
  {{#each interface.functions as |function|}}
    {{#if (not function.starts_interaction?)}}
    fn {{function.rust_name}}(
        &self,
      {{#each function.params.fields as |param|}}
        arg_{{param.name}}: {{#partial rpc.arg field=param}},
      {{/each}}
    ) -> {{#partial rpc.client_return_type function=function service=interface}} {
        let rpc_options = T::RpcOptions::default();
        self._{{function.name}}_impl(
      {{#each function.params.fields as |param|}}
            arg_{{param.name}},
      {{/each}}
            rpc_options,
        )
    }
    {{#else}}

    fn {{function.name}}(
        &self,
    ) -> ::std::result::Result<{{program.crate}}::client::{{function.interaction_name}}Client, ::anyhow::Error> {
        ::std::result::Result::Ok(
            ::std::sync::Arc::new(
                {{program.crate}}::client::{{function.interaction_name}}Impl::<P, T, S>::new(
                    self.transport().create_interaction(c"{{function.interaction_name}}")?,
                    &Self::NAMES_{{function.interaction_name}},
                )
            )
        )
    }
    {{/if (not function.starts_interaction?)}}
  {{/each}}
}

impl<P, T, S> {{interface.rust_name}}Ext<T> for {{interface.rust_name}}Impl<P, T, S>
where
    P: ::fbthrift::Protocol,
    T: ::fbthrift::Transport,
    {{! require P::Frame and T to have compatible DecBuf and EncBuf::Final }}
    P::Frame: ::fbthrift::Framing<DecBuf = ::fbthrift::FramingDecoded<T>>,
    ::fbthrift::ProtocolEncoded<P>: ::fbthrift::BufMutExt<Final = ::fbthrift::FramingEncodedFinal<T>>,
    P::Deserializer: ::std::marker::Send,
    S: ::fbthrift::help::Spawner,
{
  {{#each interface.functions as |function|}}
    {{#if (not function.starts_interaction?)}}
    fn {{function.rust_name}}_with_rpc_opts(
        &self,
      {{#each function.params.fields as |param|}}
        arg_{{param.name}}: {{#partial rpc.arg field=param}},
      {{/each}}
        rpc_options: T::RpcOptions,
    ) -> {{#partial rpc.client_return_type function=function service=interface}} {
        self._{{function.name}}_impl(
      {{#each function.params.fields as |param|}}
            arg_{{param.name}},
      {{/each}}
            rpc_options,
        )
    }
    {{/if (not function.starts_interaction?)}}
  {{/each}}
  {{#if (or interface.interaction? (array.empty? interface.all_parent_services))}}

    fn transport(&self) -> &T {
        self.transport()
    }
  {{/if (or interface.interaction? (array.empty? interface.all_parent_services))}}
}

  {{#if interface.interaction?}}
pub type {{interface.rust_name}}DynClient = dyn {{interface.rust_name}} + ::std::marker::Send + ::std::marker::Sync + 'static;
pub type {{interface.rust_name}}Client = ::std::sync::Arc<{{interface.rust_name}}DynClient>;

  {{#else}}
#[derive(Clone)]
pub struct make_{{interface.rust_name}};

/// To be called by user directly setting up a client. Avoids
/// needing ClientFactory trait in scope, avoids unidiomatic
/// make_Trait name.
///
/// ```
/// # const _: &str = stringify! {
/// use bgs::client::BuckGraphService;
///
/// let protocol = BinaryProtocol::new();
/// let transport = HttpClient::new();
/// let client = <dyn BuckGraphService>::new(protocol, transport);
/// # };
/// ```
impl dyn {{interface.rust_name}} {
    {{#each interface.unstructured_annotations as |annotation|}}
      {{#if (not (string.empty? annotation.value))}}
    pub const {{(rust_annotation_name annotation.key)}}: &'static ::std::primitive::str = {{(rust_quote annotation.value escape_backslashes=true)}};
      {{/if (not (string.empty? annotation.value))}}
    {{/each}}
    pub fn new<P, T>(
        protocol: P,
        transport: T,
    ) -> ::std::sync::Arc<impl {{interface.rust_name}} + ::std::marker::Send + ::std::marker::Sync + 'static>
    where
        P: ::fbthrift::Protocol<Frame = T>,
        T: ::fbthrift::Transport,
        P::Deserializer: ::std::marker::Send,
    {
        let spawner = ::fbthrift::help::NoopSpawner;
        Self::with_spawner(protocol, transport, spawner)
    }

    pub fn with_spawner<P, T, S>(
        protocol: P,
        transport: T,
        spawner: S,
    ) -> ::std::sync::Arc<impl {{interface.rust_name}} + ::std::marker::Send + ::std::marker::Sync + 'static>
    where
        P: ::fbthrift::Protocol<Frame = T>,
        T: ::fbthrift::Transport,
        P::Deserializer: ::std::marker::Send,
        S: ::fbthrift::help::Spawner,
    {
        let _ = protocol;
        let _ = spawner;
        ::std::sync::Arc::new({{interface.rust_name}}Impl::<P, T, S>::new(transport))
    }
}

impl<T> dyn {{interface.rust_name}}Ext<T>
where
    T: ::fbthrift::Transport,
{
    pub fn new<P>(
        protocol: P,
        transport: T,
    ) -> ::std::sync::Arc<impl {{interface.rust_name}}Ext<T> + ::std::marker::Send + ::std::marker::Sync + 'static>
    where
        P: ::fbthrift::Protocol<Frame = T>,
        P::Deserializer: ::std::marker::Send,
    {
        let spawner = ::fbthrift::help::NoopSpawner;
        Self::with_spawner(protocol, transport, spawner)
    }

    pub fn with_spawner<P, S>(
        protocol: P,
        transport: T,
        spawner: S,
    ) -> ::std::sync::Arc<impl {{interface.rust_name}}Ext<T> + ::std::marker::Send + ::std::marker::Sync + 'static>
    where
        P: ::fbthrift::Protocol<Frame = T>,
        P::Deserializer: ::std::marker::Send,
        S: ::fbthrift::help::Spawner,
    {
        let _ = protocol;
        let _ = spawner;
        ::std::sync::Arc::new({{interface.rust_name}}Impl::<P, T, S>::new(transport))
    }
}

pub type {{interface.rust_name}}DynClient = <make_{{interface.rust_name}} as ::fbthrift::ClientFactory>::Api;
pub type {{interface.rust_name}}Client = ::std::sync::Arc<{{interface.rust_name}}DynClient>;

/// The same thing, but to be called from generic contexts where we are
/// working with a type parameter `C: ClientFactory` to produce clients.
impl ::fbthrift::ClientFactory for make_{{interface.rust_name}} {
    type Api = dyn {{interface.rust_name}} + ::std::marker::Send + ::std::marker::Sync + 'static;

    fn with_spawner<P, T, S>(protocol: P, transport: T, spawner: S) -> ::std::sync::Arc<Self::Api>
    where
        P: ::fbthrift::Protocol<Frame = T>,
        T: ::fbthrift::Transport,
        P::Deserializer: ::std::marker::Send,
        S: ::fbthrift::help::Spawner,
    {
        <dyn {{interface.rust_name}}>::with_spawner(protocol, transport, spawner)
    }
}
  {{/if interface.interaction?}}
