(* This file defines the "hh-sys-config" library. It is here because this
   directory is not copied to the Flow tree, and Flow redefines this library
   and the corresponding "sysConfig.ml" file. *)

begin library "ROOTPROJECT"
  files = []
end

