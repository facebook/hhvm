error: Naming[2136] The directory `1bad` cannot name a member of the implicit package family `prototypes`; an implicit package member directory must be a valid identifier [1]
-> The implicit package family `prototypes` is declared here [2]

invalid_member_dir.php--prototypes/1bad/a.php:1:1
 1 | <?hh
   | ^ [1]
 2 | // Leading digit: not a valid identifier.
 3 | const int BAD_DIGIT_C = 1;

PACKAGES.toml:18:20
16 | # (prototypes.<dir>). Members are isolated from each other but all hard-include
17 | # intern (and transitively shared) and soft-include standalone.
18 | [implicit_packages.prototypes]
   |                    ^^^^^^^^^^ [2]
19 | path = "//prototypes/"
20 | includes = ["intern", "shared"]

error: Naming[2136] The directory `:xhp` cannot name a member of the implicit package family `prototypes`; an implicit package member directory must be a valid identifier [1]
-> The implicit package family `prototypes` is declared here [2]

invalid_member_dir.php--prototypes/:xhp/b.php:1:1
 1 | <?hh
   | ^ [1]
 2 | // XHP-style name: `:` is not a name character.
 3 | const int BAD_XHP_C = 1;

PACKAGES.toml:18:20
16 | # (prototypes.<dir>). Members are isolated from each other but all hard-include
17 | # intern (and transitively shared) and soft-include standalone.
18 | [implicit_packages.prototypes]
   |                    ^^^^^^^^^^ [2]
19 | path = "//prototypes/"
20 | includes = ["intern", "shared"]

2 errors found
