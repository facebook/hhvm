(library
  (name parentimpl)
  (wrapped false)
  (modules)
  (foreign_stubs (language c) (names stubparentimpl)))

(library
  (name parent)
  (wrapped false)
  (foreign_stubs (language c) (names libparent))
  (libraries
    parentimpl))
