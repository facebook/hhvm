(library
  (name global_autocomplete)
  (wrapped false)
  (modules
    autocomplete))

(library
  (name global_config)
  (wrapped false)
  (modules
    globalConfig)
  (libraries
    core_kernel
    find
    heap_shared_mem
    sys_utils))
