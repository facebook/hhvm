{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}{{!

Python wrappers of the structs defined in the services files. This file is
compiled into it's own module to be included by clients and services and
end-user code. It's one of the more complicated files, as it has to map
Pythonic APIs to C++ objects and back.

One of the nastier things in this file is the definition of containers.
A separate container wrapper has to be defined for each type of contained
attribute because Cython can't template C++ classes. So, for example, we need
a List__int16 or a List__string or a Map__string_mystruct instance for each
container/type combination. Consider that containers can contain other containers
or structs that contain containers and you realize how messy this can get.
Further, we'd prefer to have the end user freed from having to know about these
container types, so we'll need to define factories for them based on what they
want to include.

}}
{{#import "common/constants" as constants}}
{{#import "common/pep484" as pep484}}
{{#import "common/stream" as stream}}
{{#import "common/types" as types}}
{{> common/auto_generated_py}}
{{#if (not program.auto_migrate?)}}
cimport cython as __cython
from cpython.object cimport PyTypeObject
from libcpp.memory cimport shared_ptr, make_shared, unique_ptr
from libcpp.optional cimport optional as __optional
from libcpp.string cimport string
from libcpp cimport bool as cbool
from libcpp.iterator cimport inserter as cinserter
from libcpp.utility cimport move as cmove
from cpython cimport bool as pbool
from cython.operator cimport dereference as deref, preincrement as inc, address as ptr_address
import thrift.py3.types
from thrift.py3.types import _IsSet as _fbthrift_IsSet
from thrift.py3.types cimport make_unique
cimport thrift.py3.types
cimport thrift.py3.exceptions
cimport thrift.python.exceptions
import thrift.python.converter
from thrift.python.types import EnumMeta as __EnumMeta
from thrift.python.std_libcpp cimport sv_to_str as __sv_to_str, string_view as __cstring_view
from thrift.python.types cimport BadEnum as __BadEnum
from thrift.py3.types cimport (
    richcmp as __richcmp,
    init_unicode_from_cpp as __init_unicode_from_cpp,
    set_iter as __set_iter,
    map_iter as __map_iter,
  {{#if program.python_capi_converter?}}
    py3_to_python as __py3_to_python,
    python_to_py3 as __python_to_py3,
  {{/if program.python_capi_converter?}}
    reference_shared_ptr as __reference_shared_ptr,
    get_field_name_by_index as __get_field_name_by_index,
    reset_field as __reset_field,
    translate_cpp_enum_to_python,
    const_pointer_cast,
    make_const_shared,
    constant_shared_ptr,
  {{#if program.inplace_migrate?}}
    deref_const as __deref_const,
  {{/if program.inplace_migrate?}}
)
from thrift.py3.types cimport _ensure_py3_or_raise, _ensure_py3_container_or_raise
cimport thrift.py3.serializer as serializer
from thrift.python.protocol cimport Protocol as __Protocol
import folly.iobuf as _fbthrift_iobuf
from folly.optional cimport cOptional
from folly.memory cimport to_shared_ptr as __to_shared_ptr
from folly.range cimport Range as __cRange

import sys
from collections.abc import Sequence, Set, Mapping, Iterable
import weakref as __weakref
import builtins as _builtins
import importlib
  {{#if program.has_stream?}}
import asyncio
from folly.coro cimport bridgeCoroTaskWith
  {{/if program.has_stream?}}
  {{#if (and program.inplace_migrate? program.needs_container_converters?)}}
cimport {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.thrift_converter as _{{#each program.py3Namespaces as |ns|}}{{ns}}_{{/each}}{{program.name}}_thrift_converter
  {{/if (and program.inplace_migrate? program.needs_container_converters?)}}
  {{#each program.includeNamespaces as |include|}}
    {{#if include.has_types?}}
cimport {{include.modulePathPeriodSeparated}}.types as _{{include.modulePathUnderscoreSeparated}}_types
import {{include.modulePathPeriodSeparated}}.types as _{{include.modulePathUnderscoreSeparated}}_types
      {{#if (and program.inplace_migrate? (or program.needs_container_converters? program.has_stream?))}}
cimport {{include.modulePathPeriodSeparated}}.thrift_converter as _{{include.modulePathUnderscoreSeparated}}_thrift_converter
      {{/if (and program.inplace_migrate? (or program.needs_container_converters? program.has_stream?))}}
    {{/if include.has_types?}}
  {{/each}}

  {{#if (not program.inplace_migrate?)}}
import {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.thrift_types as _fbthrift_python_types
    {{#if program.enums?}}
from {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.thrift_enums import (
      {{#each program.enums as |enum|}}
    {{enum.name}},
      {{/each}}
)
    {{/if program.enums?}}
    {{#if program.has_visible_union_types?}}
from {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.types_impl_FBTHRIFT_ONLY_DO_NOT_USE import (
      {{#each program.py3_visible_structured_definitions as |struct|}}
        {{#if struct.union?}}
    __{{struct.name}}Type,
        {{/if struct.union?}}
      {{/each}}
)
    {{/if program.has_visible_union_types?}}

    {{#if program.has_container_types?}}
from {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.containers_FBTHRIFT_ONLY_DO_NOT_USE import (
      {{#each program.container_types as |type|}}
    {{type.flat_name}},
      {{/each}}
)
    {{/if program.has_container_types?}}
  {{#else}}

    {{#if program.has_stream?}}
cimport {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.thrift_converter as _{{#each program.py3Namespaces as |ns|}}{{ns}}_{{/each}}{{program.name}}_thrift_converter
    {{/if program.has_stream?}}

import {{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.types_inplace_FBTHRIFT_ONLY_DO_NOT_USE as _fbthrift_types_inplace
    {{#each program.py3_visible_structured_definitions as |struct|}}
{{struct.name}} = _fbthrift_types_inplace.{{struct.name}}
    {{/each}}
    {{#each program.enums as |enum|}}
{{enum.name}} = _fbthrift_types_inplace.{{enum.name}}
    {{/each}}
    {{#each program.container_types as |type|}}
{{type.flat_name}} = _fbthrift_types_inplace.{{type.flat_name}}
    {{/each}}
    {{#each program.consts as |constant|}}
{{constant.name}} = _fbthrift_types_inplace.{{constant.name}}
    {{/each}}
  {{/if (not program.inplace_migrate?)}}

  {{#if (not program.inplace_migrate?)}}
_fbthrift__module_name__ = "{{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.types"

cdef object get_types_reflection():
    return importlib.import_module(
        "{{#each program.py3Namespaces as |ns|}}{{ns}}.{{/each}}{{program.name}}.types_reflection"
    )

    {{#each program.py3_visible_structured_definitions as |struct|}}
      {{#if (not struct.union?)}}
@__cython.auto_pickle(False)
        {{#if (not struct.allow_inheritance?)}}
@__cython.final
        {{/if (not struct.allow_inheritance?)}}
cdef class {{struct.name}}({{#partial types.python_struct_class struct=struct}}):
    __module__ = _fbthrift__module_name__

    def __init__({{struct.name}} self,{{#if struct.exception?}} *args,{{/if}} **kwargs):
        self.{{#partial types.cpp_obj}} = make_shared[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}]()
        self._fields_setter = _fbthrift_types_fields.__{{struct.name}}_FieldsSetter._fbthrift_create(self.{{#partial types.cpp_obj}}.get())
        super().__init__({{#if struct.exception?}} *args, {{/if}}**kwargs)
        {{#if (and (not struct.exception?) (not struct.cpp_noncopyable?))}}

    def __call__({{struct.name}} self, **kwargs):
          {{#if (array.empty? struct.py3_fields)}}
        return self
          {{#else}}
        if not kwargs:
            return self
        cdef {{struct.name}} __fbthrift_inst = {{struct.name}}.__new__({{struct.name}})
        __fbthrift_inst.{{#partial types.cpp_obj}} = make_shared[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}](deref(self.{{#partial types.cpp_obj}}))
        __fbthrift_inst._fields_setter = _fbthrift_types_fields.__{{struct.name}}_FieldsSetter._fbthrift_create(__fbthrift_inst.{{#partial types.cpp_obj}}.get())
        for __fbthrift_name, _fbthrift_value in kwargs.items():
            (<thrift.py3.types.Struct>__fbthrift_inst)._fbthrift_set_field(__fbthrift_name, _fbthrift_value)
        return __fbthrift_inst
          {{/if (array.empty? struct.py3_fields)}}
        {{/if (and (not struct.exception?) (not struct.cpp_noncopyable?))}}

    cdef void _fbthrift_set_field(self, str name, object value) except *:
        self._fields_setter.set_field(name.encode("utf-8"), value)

    cdef object _fbthrift_isset(self):
        return _fbthrift_IsSet("{{struct.name}}", {
        {{#each struct.py3_fields as |field|}}
          {{#if (and field.has_ref_accessor? (not field.terse?))}}
          "{{field.py_name}}": deref(self.{{#partial types.cpp_obj}}).{{field.py_name}}_ref().has_value(),
          {{/if (and field.has_ref_accessor? (not field.terse?))}}
        {{/each}}
        })

    @staticmethod
    cdef _create_FBTHRIFT_ONLY_DO_NOT_USE(shared_ptr[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}] cpp_obj):
        __fbthrift_inst = <{{struct.name}}>{{struct.name}}.__new__({{struct.name}}{{#if struct.exception?}}, (<bytes>deref(cpp_obj).what()).decode('utf-8'){{/if}})
        __fbthrift_inst.{{#partial types.cpp_obj}} = cmove(cpp_obj)
        {{#if struct.exception?}}
        _builtins.Exception.__init__(__fbthrift_inst, *(v for _, v in __fbthrift_inst))
        {{/if struct.exception?}}
        return __fbthrift_inst

        {{#each struct.py3_fields as |field|}}
    cdef inline {{field.py_name}}_impl(self):
          {{#if (and field.optional? (not field.hasDefaultValue?) field.has_ref_accessor? (not field.terse?))}}
        if not deref(self.{{#partial types.cpp_obj}}).{{field.py_name}}_ref().has_value():
            return None
          {{/if (and field.optional? (not field.hasDefaultValue?) field.has_ref_accessor? (not field.terse?))}}
        {{#partial types.cython_struct_getter field=field}}

    @property
    def {{field.py_name}}(self):
        return self.{{field.py_name}}_impl()

        {{/each}}

    def __hash__({{struct.name}} self):
        return super().__hash__()

    def __repr__({{struct.name}} self):
        return super().__repr__()

    def __str__({{struct.name}} self):
        {{#if (and struct.exception? (object.notnull? struct.message_field))}}
        field = self.{{struct.message_field.py_name}}
        if field is None:
            {{! optional field, stringify }}
            return str(field)
        return field
        {{#else}}
        return super().__str__()
        {{/if (and struct.exception? (object.notnull? struct.message_field))}}


      {{#else}}


@__cython.auto_pickle(False)
@__cython.final
cdef class {{struct.name}}(thrift.py3.types.Union):
    __module__ = _fbthrift__module_name__
    Type = __{{struct.name}}Type

    def __init__(
        self, *{{#each struct.py3_fields as |field|}},
        {{#if (not field.type.structured?)}}{{!
            }}{{#if field.type.cythonTypeNoneable?}}{{!
                }}{{#partial types.cython_python_type type=field.type}} {{!
            }}{{/if field.type.cythonTypeNoneable?}}{{!
        }}{{/if (not field.type.structured?)}}{{!
            }}{{field.py_name}}=None{{!
        }}{{/each}}
    ):
        {{#each struct.py3_fields as |field|}}
          {{#if (not field.type.container?)}}
            {{#if (and (not field.type.cythonTypeNoneable?) (not field.type.structured?))}}
        if {{field.py_name}} is not None:
            if not isinstance({{field.py_name}}, {{#partial types.python_is_instance_type type=field.type}}):
                raise TypeError(f'{{field.py_name}} is not a { {{#partial types.python_type type=field.type}} !r}.')

            {{/if (and (not field.type.cythonTypeNoneable?) (not field.type.structured?))}}
            {{#if field.type.integer?}}
            {{! inject cython int overflow checks }}
            {{field.py_name}} = <{{#partial types.cython_python_type type=field.type}}> {{field.py_name}}

            {{/if field.type.integer?}}
            {{#if field.type.structured?}}
        {{field.py_name}} = _ensure_py3_or_raise({{field.py_name}}, "{{field.py_name}}", {{#partial types.python_is_instance_type type=field.type}})

            {{/if field.type.structured?}}
          {{/if (not field.type.container?)}}
          {{#if field.type.is_container_of_struct?}}
        {{field.py_name}} = _ensure_py3_container_or_raise({{field.py_name}}, {{field.type.flat_name}})

          {{/if field.type.is_container_of_struct?}}
        {{/each}}
        self.{{#partial types.cpp_obj}} = __to_shared_ptr(cmove({{struct.name}}._make_instance(
          NULL,{{#each struct.py3_fields as |field|}}
          {{field.py_name}},{{/each}}
        )))
        self._initialize_py()

    @staticmethod
    def fromValue(value):
        if value is None:
            return {{struct.name}}()
        {{! We do this with strict types first, then we will do int to float conversion}}
        {{#each struct.py3_fields as |field|}}
        if isinstance(value, {{#partial types.python_type type=field.type}}):
          {{#if field.type.number?}}
            if not isinstance(value, pbool):
                try:
            {{#if field.type.integer?}}
                    {{! Cython does OverflowError checking for us }}
                    <{{#partial types.cython_python_type type=field.type}}> value
            {{/if field.type.integer?}}
            {{#if field.type.float?}}
                    {{! This will probably fail most of the time
                        if it does then when we try again to use floating point
                        below it will just accept the loss of precision,
                        or just maybe there is a double field comming up }}
                    if <{{#partial types.cython_python_type type=field.type}}>value != value:
                        raise OverflowError
            {{/if field.type.float?}}
                    return {{struct.name}}({{field.py_name}}=value)
                except OverflowError:
                    pass
          {{#else}}
            return {{struct.name}}({{field.py_name}}=value)
          {{/if field.type.number?}}
        {{/each}}
        {{#each struct.py3_fields as |field|}}
          {{#if field.type.floating_point?}}
        if isinstance(value, {{#partial types.python_is_instance_type type=field.type}}):
            try:
                <{{#partial types.cython_python_type type=field.type}}> value
                return {{struct.name}}({{field.py_name}}=value)
            except OverflowError:
                pass
          {{/if field.type.floating_point?}}
        {{/each}}
        raise ValueError(f"Unable to derive correct union field for value: {value}")

    @staticmethod
    cdef unique_ptr[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}] _make_instance(
        {{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}* base_instance{{#each struct.py3_fields as |field|}},
        {{#if field.type.cythonTypeNoneable?}}{{#partial types.cython_python_type type=field.type}}{{#else}}object{{/if}} {{field.py_name}}{{!
        }}{{/each}}
    ) except *:
        cdef unique_ptr[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}] c_inst = make_unique[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}]()
        cdef bint any_set = False
        {{#each struct.py3_fields as |field|}}
        if {{field.py_name}} is not None:
            if any_set:
                raise TypeError("At most one field may be set when initializing a union")
            {{#partial types.cython_union_assign_field field=field}}

            any_set = True
        {{/each}}
        # in C++ you don't have to call move(), but this doesn't translate
        # into a C++ return statement, so you do here
        return cmove(c_inst)

    @staticmethod
    cdef _create_FBTHRIFT_ONLY_DO_NOT_USE(shared_ptr[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}] cpp_obj):
        __fbthrift_inst = <{{struct.name}}>{{struct.name}}.__new__({{struct.name}})
        __fbthrift_inst.{{#partial types.cpp_obj}} = cmove(cpp_obj)
        __fbthrift_inst._initialize_py()
        return __fbthrift_inst

        {{#each struct.py3_fields as |field|}}
    @property
    def {{field.py_name}}({{struct.name}} self not None):
        if self.type_int != {{field.id}}:
            {{! TODO: python 3.10 adds some more fields to AttributeError, those should be added here at some point }}
            raise AttributeError(f'Union contains a value of type {self.type.name}, not {{field.py_name}}')
        return self.value

        {{/each}}

    def __hash__({{struct.name}} self):
        return  super().__hash__()

    @property
    def type({{struct.name}} self not None):
        if self.py_type is None:
            self.py_type = {{struct.name}}.Type(self.type_int)
        return self.py_type

    @property
    def value({{struct.name}} self not None):
        if self.py_value is not None or self.type_int == 0:
            return self.py_value
        {{#each struct.py3_fields as |field|}}
        elif self.type_int == {{field.id}}:
            {{#partial types.cython_union_getter field=field}}
        {{/each}}
        return self.py_value

    {{! initialize python cache}}
    cdef _initialize_py({{struct.name}} self):
        self.py_type = None
        self.type_int = int(deref(self.{{#partial types.cpp_obj}}).getType())
        self.py_value = None

      {{/if (not struct.union?)}}
      {{! Below are some things that are common to structs and unions: }}
    def __copy__({{struct.name}} self):
        return self{{! Thrift-py3 has immutable semantics}}
      {{#if struct.cpp_noncomparable?}}

    def __eq__({{struct.name}} self, other):
        return isinstance(other, {{struct.name}}) and self._fbthrift_noncomparable_eq(other)
      {{/if struct.cpp_noncomparable?}}
      {{#if (not struct.is_struct_orderable?)}}

    def __eq__({{struct.name}} self, other):
        if not isinstance(other, {{struct.name}}):
            return False
        return deref(self.{{#partial types.cpp_obj}}.get()) == deref((<{{struct.name}}>other).{{#partial types.cpp_obj}}.get())

    def __ne__({{struct.name}} self, other):
        if not isinstance(other, {{struct.name}}):
            return True
        return deref(self.{{#partial types.cpp_obj}}) != deref((<{{struct.name}}>other).{{#partial types.cpp_obj}})
      {{#else if (not struct.cpp_noncomparable?)}}

    def __richcmp__(self, other, int op):
        r = self._fbthrift_cmp_sametype(other, op)
        return __richcmp[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}](
            self.{{#partial types.cpp_obj}},
            (<{{struct.name}}>other).{{#partial types.cpp_obj}},
            op,
        ) if r is None else r
      {{/if (not struct.is_struct_orderable?)}}

    @staticmethod
    def __get_reflection__():
        return get_types_reflection().get_reflection__{{struct.name}}()

    @staticmethod
    def __get_metadata__():
        cdef __fbthrift_cThriftMetadata meta
      {{#if struct.exception?}}
        {{> types/current_module_cbindings}}.ExceptionMetadata[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}].gen(meta)
      {{#else}}
        {{> types/current_module_cbindings}}.StructMetadata[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}].gen(meta)
      {{/if struct.exception?}}
        return __MetadataBox.box(cmove(meta))

    @staticmethod
    def __get_thrift_name__():
        return "{{program.name}}.{{struct.name}}"

      {{#if struct.has_hidden_fields?}}
    __fbthrift_field_name_list = [
        {{#each struct.py3_fields as |field|}}
        '{{field.py_name}}',
        {{/each}}
    ]

    @classmethod
    def _fbthrift_get_field_name_by_index(cls, idx):
        return cls.__fbthrift_field_name_list[idx]
      {{#else}}
    @classmethod
    def _fbthrift_get_field_name_by_index(cls, idx):
        return __sv_to_str(__get_field_name_by_index[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}](idx))
      {{/if struct.has_hidden_fields?}}

    @classmethod
    def _fbthrift_get_struct_size(cls):
        return {{(array.len struct.py3_fields)}}

    cdef _fbthrift_iobuf.IOBuf _fbthrift_serialize({{struct.name}} self, __Protocol proto):
        cdef unique_ptr[_fbthrift_iobuf.cIOBuf] data
        with nogil:
            data = cmove(serializer.cserialize[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}](self.{{#partial types.cpp_obj}}.get(), proto))
        return _fbthrift_iobuf.from_unique_ptr(cmove(data))

    cdef cuint32_t _fbthrift_deserialize({{struct.name}} self, const _fbthrift_iobuf.cIOBuf* buf, __Protocol proto) except? 0:
        cdef cuint32_t needed
        {{!
            This is a special case, we need to construct an empty _cpp_obj because
            thrift.py3.serializer.deserialize will just call __new__ to skip
            all of our runtime type checks. We do it like this because
            thrift.py3.serializer.deserialize does not have enough type information
            to call the staticmethod ._create_FBTHRIFT_ONLY_DO_NOT_USE()
        }}
        self.{{#partial types.cpp_obj}} = make_shared[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}]()
        with nogil:
            needed = serializer.cdeserialize[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}](buf, self.{{#partial types.cpp_obj}}.get(), proto)
      {{#if struct.union?}}
        # clear cache reload since the underlying data's changed
        self._initialize_py()
      {{/if struct.union?}}
        return needed

      {{#if program.python_capi_converter?}}
    @staticmethod
    def from_python(object obj):
        cdef shared_ptr[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}] cpp_obj = {{!
            }}__python_to_py3[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}, _fbthrift__NamespaceTag](obj)
        return {{struct.name}}._create_FBTHRIFT_ONLY_DO_NOT_USE(cmove(cpp_obj))
      {{/if program.python_capi_converter?}}

    def _to_python(self):
      {{#if program.python_capi_converter?}}
        return __py3_to_python[{{> types/current_module_cbindings}}.{{#partial types.c_binding node=struct}}, {{!
            }}_fbthrift__NamespaceTag](self.{{#partial types.cpp_obj}})
      {{#else}}
        return thrift.python.converter.to_python_struct(
            _fbthrift_python_types.{{struct.name}},
            self,
        )
      {{/if program.python_capi_converter?}}

    def _to_py3(self):
        return self

    def _to_py_deprecated(self):
        import thrift.util.converter
        py_deprecated_types = importlib.import_module("{{program.py_deprecated_module_path}}.ttypes")
        return thrift.util.converter.to_py_struct(py_deprecated_types.{{struct.name}}, self)

    {{/each}}
    {{#each program.container_types as |type|}}
{{> types/container_converter_impl }}

    {{/each}}

    {{#each program.consts as |constant|}}
{{constant.name}} = {{#partial constants.constant_value constant=constant}}
    {{/each}}
  {{/if (not program.inplace_migrate?)}}
  {{#each program.py3_visible_typedefs as |typedef|}}
{{typedef.name}} = {{#partial types.python_type type=typedef}}
  {{/each}}
  {{#if program.has_stream?}}

    {{#each program.stream_types as |type|}}
cdef class ClientBufferedStream__{{type.flat_name}}(ClientBufferedStream):

    @staticmethod
    cdef _fbthrift_create(cClientBufferedStream[{{#partial types.cython_cpp_type type=type}}]& c_obj, __RpcOptions rpc_options):
        __fbthrift_inst = ClientBufferedStream__{{type.flat_name}}(rpc_options)
        __fbthrift_inst._gen = make_unique[cClientBufferedStreamWrapper[{{#partial types.cython_cpp_type type=type}}]](c_obj)
        return __fbthrift_inst

    @staticmethod
    cdef void callback(
        cFollyTry[__cOptional[{{#partial types.cython_cpp_type type=type}}]]&& result,
        PyObject* userdata,
    ) noexcept:
        cdef __cOptional[{{#partial types.cython_cpp_type type=type}}] opt_val
        cdef {{#partial types.cython_cpp_type type=type}} _value
        stream, pyfuture, rpc_options = <object> userdata
        if {{#each program.stream_exceptions as |type|}}{{!
        }}result.hasException[{{#partial types.cython_cpp_type type=type}}]():
            pyfuture.set_exception({{!
            }}{{#if program.inplace_migrate?}}{{#if type.structured?}}{{!
                }}{{#partial types.python_type type=type}}.from_python({{!
                }}{{type.capi_converter_path}}.{{type.true_type.name}}_from_cpp({{!
                }}thrift.py3.exceptions.unwrap_exception[{{#partial types.cython_cpp_type type=type}}](result.exception()))){{!
            }}{{/if type.structured?}}{{#else}}{{!
                }}{{#partial types.cython_python_type type=type}}._create_FBTHRIFT_ONLY_DO_NOT_USE({{!
                }}thrift.py3.exceptions.try_make_shared_exception[{{#partial types.cython_cpp_type type=type}}](result.exception())){{!
            }}{{/if program.inplace_migrate?}}{{!
            }})
        elif {{/each}}{{!
        }}result.hasException():
            pyfuture.set_exception(
                thrift.python.exceptions.create_py_exception(result.exception(), <__RpcOptions>rpc_options)
            )
        else:
            opt_val = result.value()
            if opt_val.has_value():
                try:
                    _value = opt_val.value()
                    pyfuture.set_result({{#partial stream.cython_cpp_value_to_python_value type=type}})
                except Exception as ex:
                    pyfuture.set_exception(ex.with_traceback(None))
            else:
                pyfuture.set_exception(StopAsyncIteration())

    def __anext__(self):
        __loop = asyncio.get_event_loop()
        __future = __loop.create_future()
        # to avoid "Future exception was never retrieved" error at SIGINT
        __future.add_done_callback(lambda x: x.exception())
        __userdata = (self, __future, self._rpc_options)
        bridgeCoroTaskWith[__cOptional[{{#partial types.cython_cpp_type type=type}}]](
            self._executor,
            deref(self._gen).getNext(),
            ClientBufferedStream__{{type.flat_name}}.callback,
            <PyObject *>__userdata,
        )
        return asyncio.shield(__future)

cdef class ServerStream__{{type.flat_name}}(ServerStream):
    pass

    {{/each}}
    {{#each program.response_and_stream_functions as |function|}}
cdef class ResponseAndClientBufferedStream__{{#partial stream.response_class_name_suffix function=function}}(ResponseAndClientBufferedStream):

    @staticmethod
    cdef _fbthrift_create(cResponseAndClientBufferedStream[{{!
            }}{{#partial types.cython_cpp_type type=function.return_type}}, {{!
            }}{{#partial types.cython_cpp_type type=function.stream.elem_type}}]& c_obj, __RpcOptions rpc_options):
        __fbthrift_inst = ResponseAndClientBufferedStream__{{#partial stream.response_class_name_suffix function=function}}()
        __fbthrift_inst._stream = ClientBufferedStream__{{function.stream.elem_type.flat_name}}._fbthrift_create(
            c_obj.stream, rpc_options,
        )
        cdef {{#partial types.cython_cpp_type type=function.return_type}} _value = c_obj.response
        __fbthrift_inst._response = {{#partial stream.cython_cpp_value_to_python_value type=function.return_type}}
        return __fbthrift_inst

    def __iter__(self):
        yield self._response
        yield self._stream

cdef class ResponseAndServerStream__{{#partial stream.response_class_name_suffix function=function}}(ResponseAndServerStream):
    pass

    {{/each}}
  {{/if program.has_stream?}}
  {{#if (and program.inplace_migrate? program.needs_container_converters?)}}
    {{#each program.container_types as |type|}}
{{> types/container_converter_impl }}

    {{/each}}
  {{/if (and program.inplace_migrate? program.needs_container_converters?)}}
{{/if (not program.auto_migrate?)}}
