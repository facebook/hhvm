// @generated by "php idl.php inc {input.idl.php} {output.inc}"

#if EXT_TYPE == 0
"xml_array", T(Variant), S(0), "xmlStr", T(String), NULL, NULL, S(0), NULL, S(16793600), "/**\n * ( excerpt from http://php.net/manual/en/function.xml-array.php )\n *\n * parse the xml string to an array\n *\n * @xmlStr     string  xml string\n *\n * @return     mixed   Returns array\n */", 

#elif EXT_TYPE == 1

#elif EXT_TYPE == 2

#elif EXT_TYPE == 3

#endif
