<?hh

<<file: __EnableUnstableFeatures('union_intersection_type_hints')>>
<<file: __EnableUnstableFeatures('case_types')>>
<<file: __EnableUnstableFeatures('like_type_hints')>>

function takes_union((TYPE#1 | TYPE#2) $e): void {}

// T201523298: nullable unions can reject directly contextualized case-type closures.
function left(): TYPE#1 { return expr#1; }
function right(): TYPE#2 { return expr#2; }

<<__EntryPoint>>
function main(): void {
    takes_union(left());
    takes_union(right());
}
