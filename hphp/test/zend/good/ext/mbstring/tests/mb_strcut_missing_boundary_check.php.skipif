<?hh
<<__EntryPoint>> function main(): void {
extension_loaded('mbstring') || die('skip');
function_exists('mb_convert_encoding') || die("skip mb_convert_encoding() is not available in this build");
}
