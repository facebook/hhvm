<?hh

// AUTOCOMPLETE 16 17

function f($func_param): void {
  $if_param = true;
  if ($if_param) {
    $if_body_var = 15;
  }

  $local_lambda_var = $lambda_param ==> {
    $lambda_var = $lambda_param;
    return $lambda_var;
  };

  $local_var = $
}
