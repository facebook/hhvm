<?php
// This test is checking that _with_ constant folding, we won't run out of stack
// while visiting the AST. It doesn't make sense with -vEval.DisableHphpcOpts=1
if (ini_get('hhvm.disable_hphpc_opts')) echo 'skip';
