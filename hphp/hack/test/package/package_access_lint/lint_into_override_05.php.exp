warning: Lint[5624] The generic parameter T is redundant because it only appears in a contravariant (input) position. Consider replacing uses of generic parameter with mixed or specifying <<__Explicit>> on the generic parameter [1]

lint_into_override_05.php--bar.php:4:27
    2 | 
    3 | // package pkg1
[1] 4 | function my_function_call<T>(T $_): void {}
    5 | function my_nested_function_call<T>(vec<T> $_): void {}
    6 | 


warning: Lint[5624] The generic parameter T is redundant because it only appears in a contravariant (input) position. Consider replacing uses of generic parameter with mixed or specifying <<__Explicit>> on the generic parameter [1]

lint_into_override_05.php--bar.php:5:34
    3 | // package pkg1
    4 | function my_function_call<T>(T $_): void {}
[1] 5 | function my_nested_function_call<T>(vec<T> $_): void {}
    6 | 
    7 | class TestClass {


warning: Lint[5655] This is a cross-boundary edge. Although the callee is accessible to the caller's package pkg1 via __PackageOverride('pkg1'), that package override is load-bearing here: without it the callee stays in package pkg2, which pkg1 cannot reach, so this edge would be an error. This edge prevents removal of the __PackageOverride and SHOULD BE REMOVED. Either move the callee into the caller's package via hg mv, use __RequirePackage (if the whole function should only be invoked from pkg2) or gate this reference via a package check (if this portion of the function is meant to be gated by an environment check). [1]

lint_into_override_05.php--bar.php:9:22
    7 | class TestClass {
    8 |   public function bar(): void {
[1] 9 |     my_function_call<MyInternType>(new MyInternType());  // should raise lint error
   10 |     my_nested_function_call<MyInternType>(vec[new MyInternType()]);  // should raise lint error on nested type
   11 |   }


warning: Lint[5655] This is a cross-boundary edge. Although the callee is accessible to the caller's package pkg1 via __PackageOverride('pkg1'), that package override is load-bearing here: without it the callee stays in package pkg2, which pkg1 cannot reach, so this edge would be an error. This edge prevents removal of the __PackageOverride and SHOULD BE REMOVED. Either move the callee into the caller's package via hg mv, use __RequirePackage (if the whole function should only be invoked from pkg2) or gate this reference via a package check (if this portion of the function is meant to be gated by an environment check). [1]

lint_into_override_05.php--bar.php:9:40
    7 | class TestClass {
    8 |   public function bar(): void {
[1] 9 |     my_function_call<MyInternType>(new MyInternType());  // should raise lint error
   10 |     my_nested_function_call<MyInternType>(vec[new MyInternType()]);  // should raise lint error on nested type
   11 |   }


warning: Lint[5655] This is a cross-boundary edge. Although the callee is accessible to the caller's package pkg1 via __PackageOverride('pkg1'), that package override is load-bearing here: without it the callee stays in package pkg2, which pkg1 cannot reach, so this edge would be an error. This edge prevents removal of the __PackageOverride and SHOULD BE REMOVED. Either move the callee into the caller's package via hg mv, use __RequirePackage (if the whole function should only be invoked from pkg2) or gate this reference via a package check (if this portion of the function is meant to be gated by an environment check). [1]

lint_into_override_05.php--bar.php:10:29
     8 |   public function bar(): void {
     9 |     my_function_call<MyInternType>(new MyInternType());  // should raise lint error
[1] 10 |     my_nested_function_call<MyInternType>(vec[new MyInternType()]);  // should raise lint error on nested type
    11 |   }
    12 | }


warning: Lint[5655] This is a cross-boundary edge. Although the callee is accessible to the caller's package pkg1 via __PackageOverride('pkg1'), that package override is load-bearing here: without it the callee stays in package pkg2, which pkg1 cannot reach, so this edge would be an error. This edge prevents removal of the __PackageOverride and SHOULD BE REMOVED. Either move the callee into the caller's package via hg mv, use __RequirePackage (if the whole function should only be invoked from pkg2) or gate this reference via a package check (if this portion of the function is meant to be gated by an environment check). [1]

lint_into_override_05.php--bar.php:10:51
     8 |   public function bar(): void {
     9 |     my_function_call<MyInternType>(new MyInternType());  // should raise lint error
[1] 10 |     my_nested_function_call<MyInternType>(vec[new MyInternType()]);  // should raise lint error on nested type
    11 |   }
    12 | }


