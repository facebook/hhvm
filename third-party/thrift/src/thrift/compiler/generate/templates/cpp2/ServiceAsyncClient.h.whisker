{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}
{{> Autogen}}
#pragma once

{{#if service.is_fast_client?}}
#include <thrift/lib/cpp2/gen/fast_client_h.h>

#include "{{service.program.include_prefix}}{{service.program.name}}_types.h"
  {{#if (object.notnull? service.extends)}}
#if __has_include("{{service.extends.program.include_prefix}}{{service.extends.name}}AsyncClient.h")
#include "{{service.extends.program.include_prefix}}{{service.extends.name}}AsyncClient.h"
#else
#include "{{service.extends.program.include_prefix}}{{service.extends.program.name}}_clients.h"
#endif
  {{/if (object.notnull? service.extends)}}
  {{#each service.program.includes_for_codegen as |include|}}
#include "{{include.program.include_prefix}}{{include.program.name}}_types.h"
  {{/each}}

{{> service_h/fast_client}}
{{#else}}
#include <thrift/lib/cpp2/gen/client_h.h>

#include "{{service.program.include_prefix}}{{service.program.name}}_types.h"
  {{#if (object.notnull? service.extends)}}
#if __has_include("{{service.extends.program.include_prefix}}{{service.extends.name}}AsyncClient.h")
#include "{{service.extends.program.include_prefix}}{{service.extends.name}}AsyncClient.h"
#else
#include "{{service.extends.program.include_prefix}}{{service.extends.program.name}}_clients.h"
#endif
  {{/if (object.notnull? service.extends)}}
  {{#each service.program.includes_for_codegen as |include|}}
#include "{{include.program.include_prefix}}{{include.program.name}}_types.h"
  {{/each}}
  {{#each service.program.cpp_includes as |cpp_include|}}
#include {{cpp_include}}
  {{/each}}
  {{#if (or service.has_stream_functions? (not (array.empty? service.interactions)))}}
#include <thrift/lib/cpp2/async/ClientBufferedStream.h>
#include <thrift/lib/cpp2/async/ClientStreamInterceptorContext.h>
  {{/if (or service.has_stream_functions? (not (array.empty? service.interactions)))}}
  {{#if (or service.has_sink_functions? (not (array.empty? service.interactions)))}}
#include <thrift/lib/cpp2/async/ClientSinkBridge.h>
#include <thrift/lib/cpp2/async/Sink.h>
  {{/if (or service.has_sink_functions? (not (array.empty? service.interactions)))}}
  {{#if (or service.has_bidirectional_streams? (not (array.empty? service.interactions)))}}
#include <thrift/lib/cpp2/async/BiDiStream.h>
  {{/if (or service.has_bidirectional_streams? (not (array.empty? service.interactions)))}}

namespace apache { namespace thrift {
  class Cpp2RequestContext;
  namespace detail { namespace ac { struct ClientRequestContext; }}
  namespace transport { class THeader; }
}}

{{> service_h/async_client}}
{{/if service.is_fast_client?}}
