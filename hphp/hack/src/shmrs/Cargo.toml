# @generated by autocargo from //hphp/hack/src/shmrs:shmrs

[package]
name = "shmrs"
version = "0.0.0"
edition = "2024"
repository = "https://github.com/facebook/hhvm"
license = "MIT"

[lib]
path = "shmrs.rs"

[dependencies]
ahash = { version = "0.8.11", features = ["runtime-rng"] }
allocator-api2 = "0.2.21"
hashbrown = { version = "0.14.5", features = ["ahash", "raw", "serde"] }
libc = "0.2.139"
nohash-hasher = "0.2"
static_assertions = "1.1.0"

[dev-dependencies]
nix = { version = "0.30.1", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "signal", "term", "time", "user", "zerocopy"] }
rand = { version = "0.8", features = ["small_rng"] }
