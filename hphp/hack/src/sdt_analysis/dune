(library
 (name sdt_analysis)
 (wrapped true)
 (flags
  (:standard -linkall))
 (libraries
  core_kernel
  provider_context
  aast_names_utils
  relative_path
  tast_env
  typing_ast
  typing_defs
  typing_env_types
  hips2)
 (preprocess
  (pps visitors.ppx ppx_deriving.std ppx_hash)))
