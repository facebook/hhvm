<?hh
<<__EntryPoint>> function main(): void {
extension_loaded('mbstring') || die('skip');
function_exists('mb_substitute_character') || die("skip mb_substitute_character() is not available in this build");
}
