<?hh

<<genList>>
function main_date_get_last_errors() :mixed{
  f('a', inout $b, $b->f('Y'));
}

<<SplFileObject>>
/* function name with simple string */
function main_entry(): \unknown {
strstr(
    //EXECUTE
    $running[$i]['float .5']->getMessage(
      GRAPHEME_EXTR_MAXCHARS(
        Y\bar,
        // array with different values for $input
        $inputs
      )
    ),
  inout $i);
}

class B {
  function main_1468() :mixed{
    $x = $this;
    self(mapWithKey($x, inout $x));
  }
}

<<__EntryPoint>>
function main(): void {
  echo "Done.\n";
}
