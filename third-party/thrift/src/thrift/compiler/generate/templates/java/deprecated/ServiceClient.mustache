{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}
{{#import "common/common" as common}}
{{#import "common/types" as types}}
{{#partial common.auto_generated}}

package {{service.program.javaPackage}};

import com.facebook.nifty.client.RequestChannel;
import com.facebook.swift.codec.*;
import com.facebook.swift.service.*;
import com.facebook.swift.service.metadata.*;
import com.facebook.thrift.client.*;
import com.facebook.thrift.util.FutureUtil;
import java.io.*;
import java.lang.reflect.Method;
import java.util.*;
import org.apache.thrift.ProtocolId;
import reactor.core.publisher.Mono;

@SwiftGenerated
@Deprecated
public class {{service.javaCapitalName}}ClientImpl extends {{#if (object.notnull? service.extends)}}{{service.extends.java_qualified_name}}ClientImpl{{#else}}AbstractThriftClient{{/if}} implements {{service.javaCapitalName}} {

    // Method Handlers
    {{#each service.single_request_functions as |func|}}
    private ThriftMethodHandler {{func.java_name}}MethodHandler;
    {{/each}}

    // Method Exceptions
    {{#each service.single_request_functions as |func|}}
    private static final Class[] {{func.java_name}}Exceptions = new Class[] {
        {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}.class, {{/each}}org.apache.thrift.TException.class};
    {{/each}}

    public {{service.javaCapitalName}}ClientImpl(
        RequestChannel channel,
        Map<Method, ThriftMethodHandler> methods,
        Map<String, String> headers,
        Map<String, String> persistentHeaders,
        List<? extends ThriftClientEventHandler> eventHandlers) {
      this("{{service.name}}", channel, methods, headers, persistentHeaders, eventHandlers);
    }

    protected {{service.javaCapitalName}}ClientImpl(
        String serviceName,
        RequestChannel channel,
        Map<Method, ThriftMethodHandler> methods,
        Map<String, String> headers,
        Map<String, String> persistentHeaders,
        List<? extends ThriftClientEventHandler> eventHandlers) {
      {{#if (object.notnull? service.extends)}}super(serviceName, channel, methods, headers, persistentHeaders, eventHandlers);{{#else}}super(serviceName, channel, headers, persistentHeaders, eventHandlers);{{/if}}

      Map<String, ThriftMethodHandler> methodHandlerMap = new HashMap<>();
      methods.forEach(
          (key, value) -> {
            methodHandlerMap.put(key.getName(), value);
          });

      // Set method handlers
      {{#each service.single_request_functions as |func|}}
      {{func.java_name}}MethodHandler = methodHandlerMap.get("{{func.java_name}}");
      {{/each}}
    }

    @java.lang.Override
    public void close() {
        super.close();
    }

    {{#each service.single_request_functions as |func|}}

    @java.lang.Override
    public {{#partial types.java_type type=func.return_type has_adapter_override=false}} {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}
        {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}},{{/if (not last?)}}{{/each}}){{!
          Exceptions
        }} throws {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}, {{/each}}org.apache.thrift.TException {
      {{#if (not func.void?)}}return {{/if (not func.void?)}}{{func.java_name}}Wrapper({{#each func.params.fields as |field|}}{{field.javaName}}, {{/each}}RpcOptions.EMPTY).getData();
    }

    @java.lang.Override
    public {{#partial types.java_type type=func.return_type has_adapter_override=false}} {{func.java_name}}({{#each func.params.fields as |field|}}
        {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
        RpcOptions rpcOptions){{!
          Exceptions
        }} throws {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}, {{/each}}org.apache.thrift.TException {
      {{#if (not func.void?)}}return {{/if (not func.void?)}}{{func.java_name}}Wrapper({{#each func.params.fields as |field|}}{{field.javaName}}, {{/each}}rpcOptions).getData();
    }

    @java.lang.Override
    public ResponseWrapper<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}
        {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}},{{/each}}
        RpcOptions rpcOptions){{!
          Exceptions
        }} throws {{#each func.exceptions as |field|}}{{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}, {{/each}}org.apache.thrift.TException {
      try {
        return FutureUtil.get(executeWrapperWithOptions({{func.java_name}}MethodHandler, {{func.java_name}}Exceptions, rpcOptions{{#each func.params.fields as |field|}}, {{field.javaName}}{{/each}}));
      } catch (Throwable t) {
        if (t instanceof org.apache.thrift.TException) {
          throw (org.apache.thrift.TException) t;
        }
        {{#each func.exceptions as |field|}}
        if (t instanceof {{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}) {
          throw ({{#partial types.boxed_type type=field.type field_hasAdapter=field.hasAdapter?}}) t;
        }
        {{/each}}
        throw new org.apache.thrift.TException(t);
      }
    }
    {{/each}}
    {{#each service.interactions as |interaction|}}

    public {{interaction.name}} create{{interaction.name}}() {
        throw new RuntimeException("create interaction is not supported");
    }

    {{/each}}
}
