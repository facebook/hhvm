{{!

  Copyright (c) Meta Platforms, Inc. and affiliates.

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.

}}
{{#import "common/common" as common}}
{{#import "common/types" as types}}
{{#partial common.auto_generated}}

package {{service.program.javaPackage}};

import com.facebook.thrift.client.*;
import java.util.*;

public class {{service.javaCapitalName}}BlockingReactiveWrapper {{#if (object.notnull? service.extends)}} extends {{service.extends.java_qualified_name}}BlockingReactiveWrapper{{/if}}
    implements {{service.javaCapitalName}}.Reactive {
    private final {{service.javaCapitalName}} _delegate;

    public {{service.javaCapitalName}}BlockingReactiveWrapper({{service.javaCapitalName}} _delegate) {
        {{#if (object.notnull? service.extends)}}super(_delegate);{{/if}}
        this._delegate = _delegate;
    }

    @java.lang.Override
    public void dispose() {
        _delegate.close();
    }

    {{#each service.single_request_functions as |func|}}
    @java.lang.Override
    public reactor.core.publisher.Mono<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}}) {
        {{#if func.voidType}}
        reactor.core.publisher.Mono<Void> _m = reactor.core.publisher.Mono.<Void>create(_sink -> {
            try {
                reactor.util.context.ContextView _contextView = _sink.contextView();
                com.facebook.nifty.core.RequestContext
                    .tryContextView(_contextView)
                    .ifPresent(com.facebook.nifty.core.RequestContexts::setCurrentContext);
                _delegate.{{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}{{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}});
                _sink.success();
            } catch (Throwable _e) {
                throw reactor.core.Exceptions.propagate(_e);
            }
        });

        if (!com.facebook.thrift.util.resources.RpcResources.isForceExecutionOffEventLoop()) {
            _m = _m.subscribeOn(com.facebook.thrift.util.resources.RpcResources.getOffLoopScheduler());
        }

        return _m;
        {{#else}}
        reactor.core.publisher.Mono<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> _m = reactor.core.publisher.Mono.create(_sink -> {
            try {
                reactor.util.context.ContextView _contextView = _sink.contextView();
                com.facebook.nifty.core.RequestContext
                    .tryContextView(_contextView)
                    .ifPresent(com.facebook.nifty.core.RequestContexts::setCurrentContext);
                _sink.success(_delegate.{{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}{{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}}));
            } catch (Throwable _e) {
                _sink.error(_e);
            }
        });

        if (!com.facebook.thrift.util.resources.RpcResources.isForceExecutionOffEventLoop()) {
            _m = _m.subscribeOn(com.facebook.thrift.util.resources.RpcResources.getOffLoopScheduler());
        }

        return _m;
        {{/if func.voidType}}
    }

    {{/each}}
    {{#each service.streaming_functions as |func|}}
    @java.lang.Override
    public reactor.core.publisher.Flux<{{#partial types.stream_return_type function=func}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}}) {
        throw new UnsupportedOperationException();
    }

    {{/each}}
    {{#each service.sink_functions as |func|}}
    @java.lang.Override
    public {{#partial types.sink_return_type function=func}} {{func.java_name}}({{#each func.params.fields as |field|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}} org.reactivestreams.Publisher<{{#partial types.boxed_type type=func.sink.elem_type field_hasAdapter=false}}> payloads) {
        throw new UnsupportedOperationException();
    }

    {{/each}}
    {{#each service.interactions as |interaction|}}
    public class {{interaction.name}}Impl implements {{interaction.name}} {
        private {{service.javaCapitalName}}.{{interaction.name}} _delegateInteraction;

        {{interaction.name}}Impl({{service.javaCapitalName}}.{{interaction.name}} delegateInteraction) {
            this._delegateInteraction = delegateInteraction;
        }

        {{#each interaction.single_request_functions as |func|}}
        @java.lang.Override
        public reactor.core.publisher.Mono<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}}) {
            {{#if func.voidType}}

                reactor.core.publisher.Mono<Void> _m = reactor.core.publisher.Mono.<Void>create(_sink -> {
                    try {
                        reactor.util.context.ContextView _contextView = _sink.contextView();
                        com.facebook.nifty.core.RequestContext
                            .tryContextView(_contextView)
                            .ifPresent(com.facebook.nifty.core.RequestContexts::setCurrentContext);
                        _delegateInteraction.{{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}{{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}});
                        _sink.success();
                    } catch (Throwable _e) {
                        throw reactor.core.Exceptions.propagate(_e);
                    }
                });

                if (!com.facebook.thrift.util.resources.RpcResources.isForceExecutionOffEventLoop()) {
                    _m = _m.subscribeOn(com.facebook.thrift.util.resources.RpcResources.getOffLoopScheduler());
                }

                return _m;
            {{#else}}
                reactor.core.publisher.Mono<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> _m = reactor.core.publisher.Mono.create(_sink -> {
                    try {
                        reactor.util.context.ContextView _contextView = _sink.contextView();
                        com.facebook.nifty.core.RequestContext
                            .tryContextView(_contextView)
                            .ifPresent(com.facebook.nifty.core.RequestContexts::setCurrentContext);
                        _sink.success(_delegateInteraction.{{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}{{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}}));
                    } catch (Throwable _e) {
                        _sink.error(_e);
                    }
                });

                if (!com.facebook.thrift.util.resources.RpcResources.isForceExecutionOffEventLoop()) {
                    _m = _m.subscribeOn(com.facebook.thrift.util.resources.RpcResources.getOffLoopScheduler());
                }

                return _m;
            {{/if func.voidType}}
        }

        @java.lang.Override
        public reactor.core.publisher.Mono<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}> {{func.java_name}}({{#each func.params.fields as |field|}}
        final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }

        @java.lang.Override
        public reactor.core.publisher.Mono<ResponseWrapper<{{#partial types.boxed_type type=func.return_type field_hasAdapter=false}}>> {{func.java_name}}Wrapper({{#each func.params.fields as |field|}}
        final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}, {{/each}}RpcOptions rpcOptions) {
            throw new UnsupportedOperationException();
        }

        {{/each}}
        {{#each interaction.streaming_functions as |func|}}
        @java.lang.Override
        public reactor.core.publisher.Flux<{{#partial types.stream_return_type function=func}}> {{func.java_name}}({{#each (array.enumerate func.params.fields with_last=true) as |i field last?|}}final {{#partial types.java_type type=field.type has_adapter_override=field.hasAdapter?}} {{field.javaName}}{{#if (not last?)}}, {{/if (not last?)}}{{/each}}) {
            throw new UnsupportedOperationException();
        }

        {{/each}}
        @java.lang.Override
        public void dispose() {}
    }

    public {{interaction.name}} create{{interaction.name}}() {
        return new {{interaction.name}}Impl(_delegate.create{{interaction.name}}());
    }

    {{/each}}
}
