// @lint-ignore-every JSONSYNTAX because the format of this file is JSONC, not strict JSON
//
// The devcontainer format is described at: https://containers.dev/implementors/json_reference/
{
  "name": "HHVM",

  // Use the Docker image described in the Dockerfile file in this repo.
  //
  // Docs:
  // https://docs.github.com/en/codespaces/setting-up-your-project-for-codespaces/adding-a-dev-container-configuration/introduction-to-dev-containers#using-a-dockerfile
  //
  // Example repo:
  // https://github.com/microsoft/vscode-dev-containers/tree/main/containers/docker-existing-dockerfile
  "dockerFile": "Dockerfile",

  "hostRequirements": {
    "storage": "64gb",
    "memory": "32gb"
  },

// cmake -DMYSQL_UNIX_SOCK_ADDR=/dev/null ..
// cd ..

}
