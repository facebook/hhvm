(library
  (name fsevents)
  (wrapped false)
  (enabled_if (= %{system} macosx))
  (c_names
    fsevents_stubs)
  (c_library_flags
    -framework CoreServices
    -framework CoreFoundation)
  (libraries))
