# @generated by autocargo from //hphp/hack/src/parser/core:parser

[package]
name = "parser"
version = "0.0.0"
edition = "2024"
repository = "https://github.com/facebook/hhvm"
license = "MIT"

[lib]
path = "lib.rs"

[dependencies]
heapless = "0.9.3"
operator = { version = "0.0.0", path = "../cargo/operator" }
parser_core_types = { version = "0.0.0", path = "../cargo/core_types" }
smallvec = { version = "1.15.2", features = ["const_generics", "impl_bincode", "serde", "specialization", "union", "write"] }
smart_constructors = { version = "0.0.0", path = "../cargo/smart_constructors" }
stack_limit = { version = "0.0.0", path = "../../utils/stack_limit" }
static_assertions = "1.1.0"
