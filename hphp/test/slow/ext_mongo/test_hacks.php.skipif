<?php
try {
    $mongo = new MongoClient('mongodb://127.0.0.1/', array(
        'connect' => true,
        'connectTimeoutMS' => 500,
    ));
    $db = $mongo->selectDB('hhvm_test');
} catch (Exception $e) {
    echo 'skip';
}
