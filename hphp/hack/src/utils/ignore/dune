(library
  (name ignore)
  (wrapped false)
  (modules filesToIgnore)
  (libraries
    hh_json
    str
    string
    (select filesToIgnore.ml from
      (facebook -> facebook/filesToIgnore.ml)
      (-> filesToIgnore.stubs.ml))))
