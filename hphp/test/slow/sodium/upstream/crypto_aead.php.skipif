<?hh
// This upstream test requires hardware-supported AES256-GCM.
<<__EntryPoint>>
function main(): void {
  if (!sodium_crypto_aead_aes256gcm_is_available()) {
    echo "skip AES256-GCM is unavailable";
  }
}
