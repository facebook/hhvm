<?hh 
<<__EntryPoint>>
function main() {
  if (!extension_loaded('dom')) exit('skip dom extension not available');
  if (!in_array('compress.zlib', stream_get_wrappers())) {
    exit('skip compress.zlib wrapper is not available');
  }
}
