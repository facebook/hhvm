(5,9)-(5,11) Assignments can no longer be used as expressions. Pull the assignment out into a separate statement.
(7,11)-(7,13) Assignments can no longer be used as expressions. Pull the assignment out into a separate statement.
(9,9)-(9,17) `await` cannot be used as an expression inside another await expression, unless unstable feature: `pipe_await` is enabled. Pull the inner `await` out into its own statement.
