/**
 * Autogenerated by Thrift
 *
 * DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
 *  @generated
 */
package com.facebook.thrift.annotation.python_deprecated;

import java.util.List;
import java.util.ArrayList;
import java.util.Map;
import java.util.HashMap;
import java.util.Set;
import java.util.HashSet;
import java.util.Collections;
import java.util.BitSet;
import java.util.Arrays;
import com.facebook.thrift.*;
import com.facebook.thrift.annotations.*;
import com.facebook.thrift.async.*;
import com.facebook.thrift.meta_data.*;
import com.facebook.thrift.server.*;
import com.facebook.thrift.transport.*;
import com.facebook.thrift.protocol.*;

/**
 * Allows custom constraint on Thrift `float` fields, i.e., 32-bit floating-
 * point values, during Python thrift struct initialization or mutation.
 * 
 * This is necessary because Python's native floating-point number type
 * (`float`) may have more precision that 32 bits. Indeed, while the exact
 * precision is implementation-specified, it typically corresponds to `double`
 * in C, i.e. 64 bits (see
 * https://docs.python.org/3/library/stdtypes.html#typesnumeric). Ensuring that
 * such native values are valid 32-bit Thrift `float`s requires them to be
 * properly constrained, by either:
 * 1. rounding them to the closest 32-bit number, if they are in range, or
 * 2. bounding them to +/-Inf if they are greater/less than the max/min
 *    representable 32-bit number.
 * 
 * By default, in the absence of this annotation, thrift-python types ensure
 * that all values assigned to (or accessed from) single precision
 * floating-point (i.e., `float` in Thrift IDL) fields have the correct
 * precision, constraining them as needed.
 * 
 * Note that NaN is *never* a valid Thrift floating point number, as specified
 * in the [Thrift Object Model](https://github.com/facebook/fbthrift/blob/main/thrift/doc/object-model/index.md#primitive-types).
 * The behavior of Thrift operations in presence of native Python NaN values is
 * left undefined.
 * 
 * The behavior may be customized using each field of this struct. The defaults
 * correspond to the default behavior if this annotation is not applied to the
 * `float` type.
 * 
 * This annotation MUST NOT be applied on fields whose [Thrift IDL Type](https://github.com/facebook/fbthrift/blob/main/thrift/doc/glossary/kinds-of-types.md#thrift-idl-types)
 * is not `float`, or a container whose item type(s) are not `float` (or
 * containers that satisfy this property, recursively).
 */
@SuppressWarnings({ "unused", "serial" })
public class ConstrainedFloat32 implements TBase, java.io.Serializable, Cloneable {
  private static final TStruct STRUCT_DESC = new TStruct("ConstrainedFloat32");
  private static final TField PRECISION_LOSS_FIELD_DESC = new TField("precision_loss", TType.I32, (short)1);
  private static final TField INF_OVERFLOW_FIELD_DESC = new TField("inf_overflow", TType.I32, (short)2);
  private static final TField NOT_A_NUMBER_FIELD_DESC = new TField("not_a_number", TType.I32, (short)3);

  /**
   * - `precision_loss`: controls handling of `float` in the range
   * (-MAX_FLOAT_32, +MAX_FLOAT_32).
   * * ALLOW_INVALID: no rounding is applied at struct creation, but MAP
   *   applies on serialization.
   * * MAP: rounded to the nearest valid 32-bit float according to IEEE754
   *   trunctation convention.
   * * REJECT: raises `TypeError` if the type is not already a 32-bit float
   * 
   * @see ConstraintLevel
   */
  public final ConstraintLevel precision_loss;
  /**
   * - `inf_overflow`: controls handling of `float` outside the range
   * [-MAX_FLOAT_32, +MAX_FLOAT_32].
   * NOTE: values that are already +/- `Inf` are unaffected by this option.
   * * ALLOW_INVALID: the value is preserved on struct creation, but MAP
   *   applies on serialization.
   * * MAP: rounded "up" to +/- Inf according to sign, according to IEEE754
   *   convention.
   * * REJECT: raises `OverflowError` if the type is outside the range
   *   [-MAX_FLOAT_32, +MAX_FLOAT_32], unless the value is already
   *    +/- `Inf`, in which case it's preserved.
   * 
   * @see ConstraintLevel
   */
  public final ConstraintLevel inf_overflow;
  /**
   * - `not_a_number`: controls handling of special `NaN` values.
   * * ALLOW_INVALID: the value is preserved on struct creation and serialization.
   * * MAP: same as ALLOW_INVALID.
   * * REJECT: raises `TypeError`for `NaN` values.
   * 
   * @see ConstraintLevel
   */
  public final ConstraintLevel not_a_number;
  public static final int PRECISION_LOSS = 1;
  public static final int INF_OVERFLOW = 2;
  public static final int NOT_A_NUMBER = 3;

  public ConstrainedFloat32(
      ConstraintLevel precision_loss,
      ConstraintLevel inf_overflow,
      ConstraintLevel not_a_number) {
    this.precision_loss = precision_loss;
    this.inf_overflow = inf_overflow;
    this.not_a_number = not_a_number;
  }

  /**
   * Performs a deep copy on <i>other</i>.
   */
  public ConstrainedFloat32(ConstrainedFloat32 other) {
    if (other.isSetPrecision_loss()) {
      this.precision_loss = TBaseHelper.deepCopy(other.precision_loss);
    } else {
      this.precision_loss = null;
    }
    if (other.isSetInf_overflow()) {
      this.inf_overflow = TBaseHelper.deepCopy(other.inf_overflow);
    } else {
      this.inf_overflow = null;
    }
    if (other.isSetNot_a_number()) {
      this.not_a_number = TBaseHelper.deepCopy(other.not_a_number);
    } else {
      this.not_a_number = null;
    }
  }

  public ConstrainedFloat32 deepCopy() {
    return new ConstrainedFloat32(this);
  }

  /**
   * - `precision_loss`: controls handling of `float` in the range
   * (-MAX_FLOAT_32, +MAX_FLOAT_32).
   * * ALLOW_INVALID: no rounding is applied at struct creation, but MAP
   *   applies on serialization.
   * * MAP: rounded to the nearest valid 32-bit float according to IEEE754
   *   trunctation convention.
   * * REJECT: raises `TypeError` if the type is not already a 32-bit float
   * 
   * @see ConstraintLevel
   */
  public ConstraintLevel getPrecision_loss() {
    return this.precision_loss;
  }

  // Returns true if field precision_loss is set (has been assigned a value) and false otherwise
  public boolean isSetPrecision_loss() {
    return this.precision_loss != null;
  }

  /**
   * - `inf_overflow`: controls handling of `float` outside the range
   * [-MAX_FLOAT_32, +MAX_FLOAT_32].
   * NOTE: values that are already +/- `Inf` are unaffected by this option.
   * * ALLOW_INVALID: the value is preserved on struct creation, but MAP
   *   applies on serialization.
   * * MAP: rounded "up" to +/- Inf according to sign, according to IEEE754
   *   convention.
   * * REJECT: raises `OverflowError` if the type is outside the range
   *   [-MAX_FLOAT_32, +MAX_FLOAT_32], unless the value is already
   *    +/- `Inf`, in which case it's preserved.
   * 
   * @see ConstraintLevel
   */
  public ConstraintLevel getInf_overflow() {
    return this.inf_overflow;
  }

  // Returns true if field inf_overflow is set (has been assigned a value) and false otherwise
  public boolean isSetInf_overflow() {
    return this.inf_overflow != null;
  }

  /**
   * - `not_a_number`: controls handling of special `NaN` values.
   * * ALLOW_INVALID: the value is preserved on struct creation and serialization.
   * * MAP: same as ALLOW_INVALID.
   * * REJECT: raises `TypeError`for `NaN` values.
   * 
   * @see ConstraintLevel
   */
  public ConstraintLevel getNot_a_number() {
    return this.not_a_number;
  }

  // Returns true if field not_a_number is set (has been assigned a value) and false otherwise
  public boolean isSetNot_a_number() {
    return this.not_a_number != null;
  }

  @Override
  public boolean equals(Object _that) {
    if (_that == null)
      return false;
    if (this == _that)
      return true;
    if (!(_that instanceof ConstrainedFloat32))
      return false;
    ConstrainedFloat32 that = (ConstrainedFloat32)_that;

    if (!TBaseHelper.equalsNobinary(this.isSetPrecision_loss(), that.isSetPrecision_loss(), this.precision_loss, that.precision_loss)) { return false; }

    if (!TBaseHelper.equalsNobinary(this.isSetInf_overflow(), that.isSetInf_overflow(), this.inf_overflow, that.inf_overflow)) { return false; }

    if (!TBaseHelper.equalsNobinary(this.isSetNot_a_number(), that.isSetNot_a_number(), this.not_a_number, that.not_a_number)) { return false; }

    return true;
  }

  @Override
  public int hashCode() {
    return Arrays.deepHashCode(new Object[] {precision_loss, inf_overflow, not_a_number});
  }

  // This is required to satisfy the TBase interface, but can't be implemented on immutable struture.
  public void read(TProtocol iprot) throws TException {
    throw new TException("unimplemented in android immutable structure");
  }

  public static ConstrainedFloat32 deserialize(TProtocol iprot) throws TException {
    ConstraintLevel tmp_precision_loss = null;
    ConstraintLevel tmp_inf_overflow = null;
    ConstraintLevel tmp_not_a_number = null;
    TField __field;
    iprot.readStructBegin();
    while (true)
    {
      __field = iprot.readFieldBegin();
      if (__field.type == TType.STOP) {
        break;
      }
      switch (__field.id)
      {
        case PRECISION_LOSS:
          if (__field.type == TType.I32) {
            tmp_precision_loss = ConstraintLevel.findByValue(iprot.readI32());
          } else {
            TProtocolUtil.skip(iprot, __field.type);
          }
          break;
        case INF_OVERFLOW:
          if (__field.type == TType.I32) {
            tmp_inf_overflow = ConstraintLevel.findByValue(iprot.readI32());
          } else {
            TProtocolUtil.skip(iprot, __field.type);
          }
          break;
        case NOT_A_NUMBER:
          if (__field.type == TType.I32) {
            tmp_not_a_number = ConstraintLevel.findByValue(iprot.readI32());
          } else {
            TProtocolUtil.skip(iprot, __field.type);
          }
          break;
        default:
          TProtocolUtil.skip(iprot, __field.type);
          break;
      }
      iprot.readFieldEnd();
    }
    iprot.readStructEnd();

    ConstrainedFloat32 _that;
    _that = new ConstrainedFloat32(
      tmp_precision_loss
      ,tmp_inf_overflow
      ,tmp_not_a_number
    );
    _that.validate();
    return _that;
  }

  public void write(TProtocol oprot) throws TException {
    validate();

    oprot.writeStructBegin(STRUCT_DESC);
    if (this.precision_loss != null) {
      oprot.writeFieldBegin(PRECISION_LOSS_FIELD_DESC);
      oprot.writeI32(this.precision_loss == null ? 0 : this.precision_loss.getValue());
      oprot.writeFieldEnd();
    }
    if (this.inf_overflow != null) {
      oprot.writeFieldBegin(INF_OVERFLOW_FIELD_DESC);
      oprot.writeI32(this.inf_overflow == null ? 0 : this.inf_overflow.getValue());
      oprot.writeFieldEnd();
    }
    if (this.not_a_number != null) {
      oprot.writeFieldBegin(NOT_A_NUMBER_FIELD_DESC);
      oprot.writeI32(this.not_a_number == null ? 0 : this.not_a_number.getValue());
      oprot.writeFieldEnd();
    }
    oprot.writeFieldStop();
    oprot.writeStructEnd();
  }

  @Override
  public String toString() {
    return toString(1, true);
  }

  @Override
  public String toString(int indent, boolean prettyPrint) {
    return TBaseHelper.toStringHelper(this, indent, prettyPrint);
  }

  public void validate() throws TException {
    // check for required fields
  }

}

