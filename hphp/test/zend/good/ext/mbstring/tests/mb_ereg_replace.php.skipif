<?hh
<<__EntryPoint>> function main(): void {
extension_loaded('mbstring') || die('skip mbstring not available');
function_exists('mb_ereg_replace') || die("skip mb_ereg_replace() is not available in this build");
}
