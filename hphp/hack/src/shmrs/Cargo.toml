# @generated by autocargo from //hphp/hack/src/shmrs:shmrs

[package]
name = "shmrs"
version = "0.0.0"
edition = "2021"
repository = "https://github.com/facebook/hhvm"
license = "MIT"

[lib]
path = "shmrs.rs"

[dependencies]
ahash = "0.8"
hashbrown = { version = "0.13.2", features = ["nightly", "raw", "serde"] }
libc = "0.2.139"
nohash-hasher = "0.2"
static_assertions = "1.1.0"

[dev-dependencies]
nix = "0.25"
rand = { version = "0.8", features = ["small_rng"] }
