(library
  (name file_provider)
  (wrapped false)
  (modules file_provider)
  (libraries
    ast
    heap_shared_mem
    relative_path
    sys_utils))

(library
  (name ast_provider)
  (wrapped false)
  (modules ast_provider)
  (libraries
    ast
    heap_shared_mem
    nast
    parser_heap
    relative_path))

(library
  (name decl_provider)
  (wrapped false)
  (modules decl_provider)
  (libraries
    typing_heap))

(library
  (name fixme_provider)
  (wrapped false)
  (modules fixme_provider)
  (libraries
    collections
    errors
    heap_shared_mem
    pos))
