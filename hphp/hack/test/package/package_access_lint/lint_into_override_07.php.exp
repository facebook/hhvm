warning: Lint[5655] This is a cross-boundary edge. Although the callee is already in the caller package pkg1 via __PackageOverride('pkg1'), cross-boundary edges contribute to the bloating of pkg1 and SHOULD BE REMOVED. Either move the callee into the caller's package via hg mv, use __RequirePackage (if the whole function should only be invoked from pkg2) or gate this reference via a package check (if this portion of the function is meant to be gated by an environment check). [1]

lint_into_override_07.php--bar.php:6:24
    4 | class WithReifiable {
    5 |   <<__Reifiable>>
[1] 6 |   const type TIntern = MyInternType;
    7 | }
    8 | 


warning: Lint[5808] This expression has a type lie that makes Hack type checker think it is a different type than what it inferred. It is achieved using a type assertion through as. This can lead to fatals or subtle and latent behaviour changes. Even if the code is correct at the moment, the type checker won't produce any signal when the prerequisites for correctness change. [1]

lint_into_override_07.php--bar.php:16:5
    14 |     $x ?as MyInternType;
    15 |     // as expression — should trigger Lint[5655]
[1] 16 |     $x as MyInternType;
    17 |   }
    18 | }


warning: Lint[5655] This is a cross-boundary edge. Although the callee is already in the caller package pkg1 via __PackageOverride('pkg1'), cross-boundary edges contribute to the bloating of pkg1 and SHOULD BE REMOVED. Either move the callee into the caller's package via hg mv, use __RequirePackage (if the whole function should only be invoked from pkg2) or gate this reference via a package check (if this portion of the function is meant to be gated by an environment check). [1]

lint_into_override_07.php--bar.php:16:11
    14 |     $x ?as MyInternType;
    15 |     // as expression — should trigger Lint[5655]
[1] 16 |     $x as MyInternType;
    17 |   }
    18 | }


